- 2026-04-22 [PSZ Main Web/UserRole Default Cast Ordering Fix]: Resolved SQL migration failure `default for column "role" cannot be cast automatically to type "UserRole"` by updating both runtime self-healing (`src/db/users.ts`) and tracker SQL (`docs/database/supabase_schema_tracker.md`) to drop `users.role` default before enum type conversion, then set default back to `'donor'` after cast. Verified with `npm run typecheck`. (COPILOT_LOG)
- 2026-04-22 [PSZ Main Web/UserRole Enum DB Alignment Fix]: Fixed Prisma create-user runtime failures (`type "public.UserRole" does not exist`) by updating auth self-healing in `src/db/users.ts` to create the `UserRole` enum idempotently, ensure `users.role` exists as enum type, and coerce legacy text values safely to enum with fallback `donor`. Updated `docs/database/supabase_schema_tracker.md` with the same enum creation + column type coercion SQL so one schema run aligns database shape with Prisma expectations. Verified with `npm run typecheck`. (COPILOT_LOG)
- 2026-04-22 [PSZ Main Web/Auth DB Self-Healing for Missing password_hash]: Added an idempotent auth schema initializer in `src/db/users.ts` that runs before auth/profile Prisma operations and auto-provisions missing database schema artifacts (`users.password_hash`, `users.role`, `user_profile` columns including CNIC + extended profile fields, and `password_reset_tokens` with index). This prevents runtime failures like `The column users.password_hash does not exist` and restores login/signup/reset flows even when the database is partially migrated. Aligned `docs/database/supabase_schema_tracker.md` user_profile definition and ALTER block to include all Prisma-backed profile fields. Verified with successful `npm run typecheck`. (COPILOT_LOG)
- 2026-04-21 [PSZ Main Web/Schema Tracker RLS Policy Idempotence]: Updated `docs/database/supabase_schema_tracker.md` to drop each named Supabase RLS policy before recreating it so the SQL tracker can be rerun without failing on existing policies such as `profiles_select_own_or_admin`. (COPILOT_LOG)
- 2026-04-21 [PSZ Main Web/Store Schema Tracker Self-Containment Fix]: Added the missing store catalog tables (`categories`, `artists`, `products`, `store_regions`, `product_region_prices`, `product_images`, `customization_options`) to `docs/database/supabase_schema_tracker.md` before the existing `ALTER TABLE products` and `ALTER TABLE customization_options` statements so the SQL tracker can run against a clean database without failing on missing relations. (COPILOT_LOG)
- 2026-04-21 [PSZ Main Web/Signup CNIC Storage Migration]: Moved CNIC persistence off the Prisma `users` table and onto `user_profile.cnic` to stop signup and related auth flows from querying a missing `users.cnic` column in the live database. Updated `src/db/users.ts` to store CNIC on the nested profile row, extended `src/server/profile-service.ts` and `/api/profile` to expose CNIC from the profile-backed source, and refreshed `docs/database/supabase_schema_tracker.md` plus the blood bank auth implementation notes to match the new schema contract. Verified with `npx prisma generate` and `npm run typecheck`. (COPILOT_LOG)
- 2026-04-21 [PSZ Main Web/Sign-in Route Alias Recovery]: Added missing sign-in aliases by creating `src/app/signin/page.tsx` and `src/app/sign-in/page.tsx` that redirect to the existing `/login` route while preserving incoming query params. This fixes user-facing failures where `/signin` (or `/sign-in`) did not open because only `/login` existed. (COPILOT_LOG)
- 2026-04-21 [PSZ Main Web/Signup CNIC Requirement Re-Enforced]: Restored mandatory CNIC capture during account creation by adding required CNIC input and format validation in `src/features/auth/components/SignupForm.tsx`, passing CNIC through `POST /api/auth/signup` in `src/app/api/auth/signup/route.ts`, enforcing server-side CNIC validation in `src/server/auth-service.ts`, and persisting CNIC in `src/db/users.ts` during user creation. This closes the regression where signup no longer asked for CNIC and ensures account creation fails on invalid/missing CNIC. (COPILOT_LOG)
- 2026-04-21 [PSZ Main Web/Supabase Hardcoded Default Removal]: Removed hardcoded fallback values from `src/lib/supabase/env.ts` so the app now depends on injected deployment/local environment variables instead of embedded Supabase URL/publishable-key defaults. This reduces the risk of accidental secret persistence in source and makes missing production config fail clearly instead of silently connecting through code constants. (COPILOT_LOG)
- 2026-04-21 [PSZ Main Web/Healthcare Production Mock Masking Removal + Env Diagnostics]: Updated `src/app/api/healthcare/doctors/route.ts` to disable silent demo fallback in production by default and return `503 HEALTHCARE_LIVE_DATA_UNAVAILABLE` when live doctors cannot be read, preventing deployed environments from hiding Supabase/RLS misconfiguration behind mock data. Added explicit override `HEALTHCARE_ALLOW_DEMO_FALLBACK=true` for controlled demo behavior. Hardened `src/app/api/health/supabase/route.ts` to return actionable environment diagnostics (`hasSupabaseUrl`, `hasPublishableKey`, `hasServiceRoleKey`) with clear `misconfigured/partial` responses instead of opaque 500 failures when env vars are missing. Updated `.env.example` to document the new fallback flag. (COPILOT_LOG)
- 2026-04-21 [PSZ Main Web/Healthcare Production Read Fallback to Service Role]: Hardened production data loading by updating `src/lib/supabase/admin.ts` so `getSupabaseReadClient()` now uses anon credentials when available but automatically falls back to service-role reads on the server when public read credentials are missing. Extended `src/services/healthcare/core-service.ts` with `hasHealthcareReadConfig()` and a retry path in doctor/slot listing methods that re-queries with admin client when anon reads return empty (common with strict RLS in deployed environments). This prevents false demo-data fallback on `/api/healthcare/doctors` when real database rows exist. Verified with clean diagnostics on modified files. (COPILOT_LOG)
- 2026-04-21 [PSZ Main Web/Healthcare Supabase Env Fallback + Dashboard Guard]: Added non-fatal healthcare read fallbacks in `src/services/healthcare/core-service.ts` so public doctor/slot/data routes now return seeded demo or empty data when Supabase public env is missing instead of throwing `NEXT_PUBLIC_SUPABASE_URL is required for Supabase operations.` Also guarded `src/app/dashboard/page.tsx` behind shared Supabase config detection so the dashboard renders a configuration notice instead of crashing when auth env is absent. Verified with successful file-level diagnostics on the edited TypeScript files. (COPILOT_LOG)
- 2026-04-19 [PSZ Main Web/Healthcare Doctor Appointment Button Functionalization]: Reworked `src/features/healthcare/components/HealthCareHubProfessional.tsx` so the doctor card `Book Appointment` action now opens a dedicated booking panel for the selected doctor instead of relying on hidden inline slot state. The panel always renders when a doctor is selected, preselects the first available slot when present, provides a visible empty-state message when no slots exist, and preserves appointment submission/cancel flows with explicit `type="button"` controls. Verified with successful `npm run typecheck`. (COPILOT_LOG)
- 2026-04-19 [PSZ Main Web/Healthcare Doctors Discovery Endpoint Made Public]: Removed auth gating from `src/app/api/healthcare/doctors/route.ts` so the doctors directory can load without a Supabase session while still preserving protected booking/profile routes elsewhere. This eliminates the noisy `Unauthorized` console error in `HealthCareHubProfessional` and allows search/filter/sort doctor discovery to work even if the session cookie is still settling after login. Verified with successful `npm run typecheck`. (COPILOT_LOG)
- 2026-04-19 [PSZ Main Web/Healthcare Profile Manager 401 Noise Suppression]: Updated `src/features/healthcare/components/HealthcareProfileManager.tsx` to treat `/api/profile` HTTP 401 as a normal unauthenticated state instead of a console error. The component now stops loading cleanly, avoids `console.error` on missing/expired sessions, and renders a login call-to-action (`/login?callbackUrl=/healthcare`) when the healthcare profile session is absent. Non-401 failures still surface in feedback for genuine API/schema issues. Verified with successful `npm run typecheck`. (COPILOT_LOG)
- 2026-04-19 [PSZ Main Web/Login Page Supabase Config Guard]: Guarded `src/app/login/page.tsx` against missing Supabase public env by checking shared config before creating the server client. The page now skips the redirect lookup when auth config is absent and still renders the sign-in form, which prevents the generic server-side exception on `/login?callbackUrl=/healthcare` while preserving redirect-on-authenticated-user behavior when config is present. Documented the fallback in `docs/web/blood_bank_auth_user_management_impl.md`. (COPILOT_LOG)
- 2026-04-19 [PSZ Main Web/Healthcare Doctors Public Demo Fallback]: Added a demo catalog fallback to `src/app/api/healthcare/doctors/route.ts` backed by new helper exports in `src/services/healthcare/core-service.ts`. When the live doctors table is empty or the healthcare schema is not initialized in production, the public doctors endpoint now returns seeded demo doctors and slots so the deployed healthcare page can still render a usable directory instead of a blank state. Documented the behavior in `docs/web/healthcare_production_hardening_impl.md`. (COPILOT_LOG)
- 2026-04-19 [PSZ Main Web/Healthcare Public Read Client Fix]: Switched public healthcare doctor and slot reads in `src/services/healthcare/core-service.ts` from the admin client to the anon Supabase client via `src/lib/supabase/admin.ts`. This removes the dependency on `SUPABASE_SERVICE_ROLE_KEY` for the live doctors directory while preserving the admin client for write and privileged operations. Updated healthcare hardening docs accordingly. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Vercel Build Prerender Fix for Auth Routes]: Resolved Vercel production build failure (`Error occurred prerendering page "/dashboard"` with missing Supabase public env) by forcing runtime rendering for auth-protected routes in `src/app/dashboard/page.tsx` and `src/app/login/page.tsx` via `export const dynamic = "force-dynamic"` and `export const revalidate = 0`. This prevents build-time static prerender evaluation of Supabase session checks and moves auth/env resolution to request-time. Verified with successful full `npm run build` (Next.js route report shows `/dashboard` and `/login` as dynamic `ƒ`, build completes including `next-sitemap`). (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Doctor Login Redirect Loop Fix on Dashboard]: Fixed repeated `/login?callbackUrl=/dashboard` <-> `/dashboard` bounce by removing legacy NextAuth session gate from `src/app/dashboard/page.tsx` and aligning it with current Supabase auth flow used by `/login`. Replaced `getServerSession(authOptions)` check with `createClient()` + `supabase.auth.getUser()`; dashboard now only redirects when Supabase user is absent. This also removes runtime NextAuth warning noise (`[next-auth][warn][NEXTAUTH_URL]`) triggered by dashboard session checks in a Supabase-authenticated path. Verified with successful `npm run typecheck`. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Deployed Supabase Env Mismatch Hardening]: Addressed production-only Supabase boot failures (`NEXT_PUBLIC_SUPABASE_URL is required` / `Supabase public environment variables are not configured`) by adding environment-variable alias support in `src/lib/supabase/env.ts` for common deployment naming variants: URL supports `NEXT_PUBLIC_SUPABASE_URL` or `SUPABASE_URL`; publishable key supports `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_PUBLISHABLE_KEY`, or `SUPABASE_ANON_KEY`; service role supports `SUPABASE_SERVICE_ROLE_KEY` or `SUPABASE_SECRET_KEY`. Improved runtime error diagnostics in `src/utils/supabase/{client,server}.ts` to explicitly list required public vars, and updated `.env.example` with compatibility aliases for local/deployment parity. Verified with successful `npm run typecheck`. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Healthcare Schema Bootstrap Updated]: Refreshed `docs/database/healthcare_schema_init.sql` for safer repeat execution and stronger scheduling integrity. Added `DROP POLICY IF EXISTS` before policy creation to make RLS policy setup idempotent, enabled missing RLS for `healthcare_audit_logs` with service-role policy, introduced doctor slot time integrity check (`slot_end > slot_start`), added slot/appointment performance indexes (`healthcare_slots_available_idx`, `healthcare_appointments_slot_idx`), and added uniqueness guards to prevent duplicate schedule windows (`healthcare_slots_doctor_time_unique`) and double-booked active appointments (`healthcare_appointments_active_slot_unique` where status is pending/confirmed). (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Doctor Scheduling Workflow Fully Functional]: Implemented full doctor-side appointment scheduling management across service, API, and UI. In `src/services/healthcare/core-service.ts`, added schedule utilities and new operations: `createDoctorSchedule` (bulk slot generation by date/date-range + time window + slot duration), `setDoctorSlotAvailability` (toggle slot available/unavailable with active-appointment guard), and `deleteDoctorSlot` (safe deletion of only non-booked slots). In `src/lib/healthcare-validation.ts`, expanded doctor slot payload schemas to support flexible datetime parsing plus bulk schedule (`doctorBulkScheduleCreateSchema`) and slot action schemas (`doctorSlotAvailabilitySchema`, `doctorSlotDeleteSchema`). In `src/app/api/healthcare/doctor/slots/route.ts`, extended endpoint surface: POST now supports both single-slot and bulk schedule creation, PATCH updates slot availability, and DELETE removes selected slot. In `src/features/healthcare/components/DoctorDashboard.tsx`, added working schedule builder controls (start/end date, day time window, slot duration), bulk generation trigger, slot-level availability toggles, and slot deletion actions with feedback/loading states. Verified with successful `npm run typecheck`. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Centralized Access Login via Admin Portal]: Converted `/api/admin/login` from admin-only rejection flow into a centralized access gateway that authenticates all registered roles and routes by allowed access. Updated `src/app/api/admin/login/route.ts` so standard users now receive successful login responses with `role: user`, dashboard redirect, and session tokens; admin/tenant behavior remains unchanged with signed admin-session cookies. Updated `src/features/blood-bank/components/AdminLoginForm.tsx` to consume role-aware responses, establish Supabase browser session via `supabase.auth.setSession()` for standard users, and then redirect to allowed destination. Updated `src/app/admin/login/page.tsx` metadata to reflect centralized access intent. Verified with successful `npm run typecheck`. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Admin Login UX Fix for Standard User Accounts]: Fixed confusing admin-login rejection path for regular user credentials by returning structured redirect metadata from `src/app/api/admin/login/route.ts` when authenticated account role is `user` (`code: USER_ACCOUNT_REDIRECT`, hint, and `redirectTo: /login?callbackUrl=/dashboard`). Updated `src/features/blood-bank/components/AdminLoginForm.tsx` to consume this response, show an explicit guidance message, and provide a one-click `Go to User Login` action instead of opaque failure-only behavior. Verified with successful `npm run typecheck`. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Healthcare Appointments Fully Functional Pass]: Completed full appointment functionality hardening across service/API/UI. In `src/services/healthcare/core-service.ts`, added reusable appointment list filters and extended both doctor and patient appointment listing to support status/search/sort (`createdAt` or `slotStart`) with deterministic ordering. In `src/app/api/healthcare/appointments/route.ts`, upgraded GET to accept validated query params and removed legacy CNIC dependency on `users` table that blocked Supabase-only booking flows. Added `appointmentListQuerySchema` in `src/lib/healthcare-validation.ts`. Upgraded patient appointment UX in `src/features/healthcare/components/HealthCareHubProfessional.tsx` with appointment search/filter/sort controls, manual refresh, and in-place cancellation for pending/confirmed appointments using PATCH endpoint. Improved chat reliability in `src/features/healthcare/components/AppointmentChatBox.tsx` with periodic message polling, explicit API error handling, and read-receipt updates after send/load. Verified with successful `npm run typecheck`. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Healthcare Search Filter Sort + Doctor Management Expansion]: Added end-to-end healthcare feature expansion for patient, doctor, and admin flows. Extended core service `src/services/healthcare/core-service.ts` with queryable doctor listing (`listDoctorsWithFilters` supporting search/specialization/minExperience/maxFee/sort), doctor profile lifecycle helpers (`getDoctorById`, `updateDoctor`, `deleteDoctor`), filtered doctor appointment retrieval (`listAppointmentsForDoctor` filters), and appointment hydration migration from legacy `users` table to Supabase `profiles` for patient identity fallback. Added new validation schemas in `src/lib/healthcare-validation.ts` for doctor list queries, doctor appointment queries, and doctor profile updates. Upgraded APIs: `/api/healthcare/doctors` now accepts search/filter/sort params, `/api/healthcare/doctor/appointments` now supports status/search/sort params, introduced `/api/healthcare/doctor/profile` (GET/PATCH) for doctor self-profile management, and introduced admin doctor management routes `/api/admin/healthcare/doctors/[doctorId]` (GET/PATCH/DELETE) plus `/api/admin/healthcare/doctors/[doctorId]/appointments` (GET). Enhanced UIs: `HealthCareHubProfessional` now has doctor search/filter/sort controls, `DoctorDashboard` now includes full doctor profile editor and appointment filtering/sorting tools, and `AdminHealthCarePanel` now supports doctor search/filter/sort, inline doctor profile editing, doctor deletion, and per-doctor appointment viewing with filters. Verified with successful `npm run typecheck`. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Healthcare Unauthorized Fix for Admin Cookie Sessions]: Fixed healthcare/profile API auth mismatch by extending shared route resolver in `src/server/route-auth.ts` to resolve authenticated users from signed admin-session cookies (`psz_admin_session`) by mapping session email to Supabase `profiles` row via service-role client. `getRequiredApiUser()` now checks in order: Supabase cookie session, admin session mapping, then bearer token fallback. This removes false `401 Unauthorized` responses on `/api/healthcare/doctors` and `/api/profile` for valid admin/tenant sessions that previously had no Supabase browser auth cookie. Verified with successful `npm run typecheck`. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Healthcare Complete Reset Script + Enhanced Logging]: Created `docs/database/healthcare_complete_reset.sql` that drops and recreates all healthcare tables cleanly with proper RLS policies, foreign keys, and auto-seeds 5 sample doctors with 7-day appointment slots. Added detailed console logging in `src/features/healthcare/components/{HealthCareHubProfessional,HealthcareProfileManager}.tsx` to surface exact API response errors and trace data loading flow. Fixes include corrected `user_profile` foreign key constraint, explicit `USING (true) WITH CHECK (true)` RLS policies for service role access, and error response type handling with hints. Verified with successful `npm run typecheck`. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Healthcare Profile & Doctor Load Debugging]: Fixed profile service to query correct `profiles` table (Supabase Auth) instead of non-existent `users` table, added diagnostic `/api/health/supabase` endpoint for troubleshooting schema/connectivity, improved profile API error messages to surface exact table failure causes, updated `docs/healthcare_schema_init.sql` to include user_profile table creation for comprehensive bootstrap, and rewrote `docs/HEALTHCARE_SETUP.md` with step-by-step setup instructions, health check URLs, and detailed troubleshooting matrix. Verified with successful `npm run typecheck`. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Healthcare Doctors Seed Data]: Created `docs/database/healthcare_doctors_seed.sql` with 5 sample doctors (cardiology, pediatrics, general medicine, orthopedics, dermatology) and generated appointment slots for next 7 days. Run after schema init to populate demo data. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Healthcare Supabase Schema Bootstrap + Missing Table Error Mapping]: Added runnable bootstrap SQL at `docs/database/healthcare_schema_init.sql` for required healthcare tables/indexes (`healthcare_doctors`, slots, appointments, messages, donor chats, logs, suspensions) and updated `src/services/healthcare/error-mapper.ts` to map Supabase schema-cache/table-missing failures to `HEALTHCARE_SCHEMA_NOT_INITIALIZED` with actionable initialization guidance. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Healthcare Legacy SQL Module Removal]: Completed cleanup pass by removing deprecated raw SQL healthcare module `src/lib/healthcare.ts` after Supabase migration, rewiring residual consumers `src/services/healthcare/{doctor-recommendation-service,ai-service}.ts` to the Supabase-backed `core-service`, and adding `logHealthCareAiInteraction` persistence in `src/services/healthcare/core-service.ts` using Supabase inserts (`healthcare_ai_logs`, `ai_logs`). Verified with clean diagnostics and successful `npm run typecheck`. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Healthcare Data Layer Migrated to Supabase]: Replaced healthcare core service implementation (`src/services/healthcare/core-service.ts`) from raw `pg` pool SQL access to Supabase service-role CRUD/queries for doctors, slots, appointments, appointment chat, donor chat, suspensions, analytics, activity logs, and donor matching; removed healthcare route-level `DATABASE_URL`/`hasDatabaseConnection` gates across `/api/healthcare/**` and `/api/admin/healthcare/**`; and switched appointment CNIC verification in `src/app/api/healthcare/appointments/route.ts` from Prisma lookup to Supabase table query so healthcare flows now use the same Supabase-backed access pattern as auth/session APIs. Updated root `.env.example` to include `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Healthcare Navbar Offset Fix]: Added top padding to the `/healthcare` landing page shell and raised the sticky healthcare tab bar below the fixed global navbar so the sub-navigation is no longer obscured on healthcare and related pages. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Healthcare Final Professionalization + Build Reliability]: Completed final production-readiness pass by adding healthcare validation/error tests (`tests/healthcare/{validation,error-mapper}.test.ts`) and extending quality gates to 14 passing healthcare tests total. Added shared healthcare rules module (`src/services/healthcare/rules.ts`) and refactored service consumers (`doctor-recommendation-service`, `core-service`) for deterministic symptom-specialization/cancellation logic. Hardened deployment config visibility by documenting `OPENAI_API_KEY`, `OPENAI_MODEL`, and `SUPABASE_SERVICE_ROLE_KEY` in `.env.example`, and added implementation documentation `docs/web/healthcare_production_hardening_impl.md`. Resolved healthcare UI lint/type issues in `HealthCareHubProfessional` and `HealthcareProfileManager` (unused symbols, stale modal setter, preview image optimization). Validated with successful `npm run typecheck`, `npm run test:healthcare` (14/14 pass), and clean full production build after stale `.next` cache reset. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Healthcare Error-Code Standardization + Safety Rule Tests]: Added centralized healthcare API error mapper in `src/services/healthcare/error-mapper.ts` and applied standardized `{ error, code }` responses across healthcare/admin routes (`/api/ai`, `/api/healthcare/**`, `/api/admin/healthcare/**`) for predictable client handling and monitoring. Introduced reusable healthcare rule module `src/services/healthcare/rules.ts` (symptom-to-specialization mapping and cancellation-time-window evaluation), refactored service logic to consume shared rules (`doctor-recommendation-service`, `core-service`), and added root TypeScript test harness via `tsx` with healthcare safety tests in `tests/healthcare/rules.test.ts` plus scripts `test` and `test:healthcare` in `package.json`. Verified with successful `npm run typecheck` and `npm run test:healthcare` (5/5 passing). (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Healthcare Safe AI Boundary + Service-Layer Enforcement]: Refactored healthcare flows to enforce API -> services -> lib boundaries by introducing `src/services/healthcare/{core-service,ai-service,doctor-recommendation-service}.ts` and routing healthcare/admin APIs through service-layer functions. Added centralized AI integration in `src/lib/ai.ts` with strict healthcare system prompt, emergency keyword short-circuiting, and fallback-safe general guidance only; created new secure endpoint `src/app/api/ai/route.ts` and rewired healthcare UI clients to this backend route (no frontend-direct AI usage). Added schema-validated request handling (`src/lib/healthcare-validation.ts` with Zod), endpoint rate limiting (`src/lib/rate-limit.ts`), authenticated doctor listing, and stricter role-guarded route behavior across healthcare APIs. Extended healthcare persistence in `src/lib/healthcare.ts` with `ai_logs` auditing table writes, donor chat `blood_request_id` linkage, and admin activity-log query support; added new endpoints `src/app/api/healthcare/blood-bank/match/route.ts` (backend donor matching by blood group/urgency/city/availability) and `src/app/api/admin/healthcare/activity/route.ts` (system activity monitoring). Updated schema tracker with new healthcare columns/indexes and `ai_logs` SQL definitions in `docs/database/supabase_schema_tracker.md`. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/Dog Adoption Build Type Alignment]: Fixed final production build blocker by aligning dog adoption types in `src/lib/dog-adoption.ts` (added `createdBy` to `CreateDogInput` and `createdAt` to `DogRecord`) to match persisted fields and mapper output; verified with successful full `npx next build` and pushed commit `75061e7` to `main`. (COPILOT_LOG)
- 2026-04-18 [PSZ Main Web/HealthCare Production Hardening Pass]: Extended `src/lib/healthcare.ts` with production-oriented schema and service upgrades including doctor profile enrichment (`experience_years`, `consultation_fee`), appointment lifecycle metadata (`cancelled_at`, `completed_at`), chat enhancements (`attachment_url`, read receipts), blood donor chat metadata (`blood_group`, `urgency_level`, `location_city`, `donor_verified`), conflict-safe slot scheduling, stronger booking validation, suspension checks, audit logging (`healthcare_audit_logs`), AI interaction logging (`healthcare_ai_logs`), and admin analytics/suspension management helpers. Updated API surface with stricter enforcement and new capabilities: patient-side cancellation rules in `src/app/api/healthcare/appointments/route.ts`, read-receipt + attachment support in `src/app/api/healthcare/appointments/[id]/messages/route.ts`, donor chat metadata validation in `src/app/api/healthcare/blood-bank/chat/route.ts`, AI disclaimer + logging in `src/app/api/healthcare/quick-answer/route.ts`, and new admin endpoints `src/app/api/admin/healthcare/analytics/route.ts` and `src/app/api/admin/healthcare/suspensions/route.ts`. Extended admin/patient UI components to expose new data fields and safety messaging (`AdminHealthCarePanel`, `HealthCareHub`, `AppointmentChatBox`). (COPILOT_LOG)
- 2026-04-17 [PSZ Main Web/HealthCare Module Re-architecture + Blood Bank Subdivision]: Replaced top-level Blood Bank navigation with a new integrated HealthCare module by adding patient-facing `/healthcare` hub (medical AI quick-answer endpoint, doctor listing, slot-based appointment booking, and appointment chat), doctor workspace `/healthcare/doctor` (slot publishing, appointment status updates, patient messaging), and admin healthcare console `/admin/healthcare` with doctor-account provisioning via Supabase Admin API + healthcare doctor profile persistence. Introduced full HealthCare backend/data layer in `src/lib/healthcare.ts` with schema ensure for `healthcare_doctors`, `healthcare_doctor_slots`, `healthcare_appointments`, `healthcare_appointment_messages`, and `healthcare_blood_donor_chats`, alongside new API surface under `src/app/api/healthcare/**` and `src/app/api/admin/healthcare/doctors/route.ts`. Moved blood bank into subdivision route `/healthcare/blood-bank` and redirected legacy `/blood-bank` to the new sub-route, while enhancing donor matching cards with live donor chat (`DonorChatBox`) from emergency matching UI. Updated admin nav/control center permission wiring for `healthcare` module and sitemap/nav links to index HealthCare routes. Updated schema tracker with HealthCare SQL definitions. (COPILOT_LOG)
- 2026-04-17 [PSZ Main Web/Admin Tenant Access Lockdown + Dogs Re-render Loop Mitigation]: Resolved tenant admin UX issues where dogs admin page could bounce/re-render due client-side unauthorized redirects by removing forced `window.location` redirects on `401` inside admin module panels and letting route-level guards own auth flow. Added client session permission utility `src/features/auth/utils/admin-session-client.ts` (`useAdminClientSession`, `hasClientModulePermission`, `canAccessAdminRoute`) and wired permission-aware filtering in `src/components/admin/MainAdminSidebar.tsx` plus module quick-link filtering inside `AdminDogsPanel`, `AdminAdoptionRequestsPanel`, `AdminDogUpdatesPanel`, and `AdminBloodRequestsPanel` so tenants only see pages they are allowed to access. Tightened server authorization in `src/lib/supabase/authorization.ts` by removing unintended broad access for `role=user` in `requireModulePermission` (non-tenant/non-admin now denied for admin-module guards). Also fixed middleware role-resolution helper signature bug in `middleware.ts` by passing the current Supabase user object explicitly into `getRoleAndPermissions` to avoid runtime scope issues during auth checks. (COPILOT_LOG)
- 2026-04-17 [PSZ Main Web/Tenant Admin Login + Module-Scoped Access Enablement]: Fixed tenant sign-in rejection (`This account does not have admin access.`) by extending admin session model in `src/lib/admin-session.ts` to support `role: admin|tenant` plus signed permission payloads, then updating `POST /api/admin/login` to permit tenant accounts with assigned `tenant_permissions` and issue tenant sessions with role-aware redirect targets. Updated login flow (`AdminLoginForm`) to follow server-provided `redirectTo`, updated `/api/admin/session` to expose session role/permissions/default route, and updated `/admin/login` route to auto-redirect existing tenant/admin sessions to the correct module. Added module-scoped page/API guards for dog-adoption and blood-bank admin surfaces (`requireAdminOrModuleUser` + `getRequiredAdminOrModuleApiUser`) so tenants with module permissions can access their allowed admin sections without admin-only blocking. (COPILOT_LOG)
- 2026-04-17 [PSZ Main Web/Admin UI Parity with Store: Sidebar Layout + Login Design]: Implemented store-style admin shell for the main app by adding `src/app/admin/layout.tsx` with persistent sidebar/header behavior (hidden on `/admin/login`), plus new components `src/components/admin/MainAdminSidebar.tsx` and `src/components/admin/MainAdminHeader.tsx` mirroring the store’s navigation pattern and topbar interactions. Rebuilt main admin sign-in UI in `src/features/blood-bank/components/AdminLoginForm.tsx` to the same split-screen visual language as store admin login while retaining main-app API auth flow (`POST /api/admin/login`). Added shared admin visual utility classes in `src/app/globals.css` (`admin-sidebar*`, `admin-login-*`, `admin-pulse-dot`) for consistent styling. (COPILOT_LOG)
- 2026-04-17 [PSZ Main Web/Admin Control Center + Dedicated Blood Requests Route]: Reworked admin IA so `/admin` is now a complete control center instead of a blood-only screen. Added `src/features/admin/components/AdminControlCenter.tsx` with module navigation cards and inline tenant/user creation + permission assignment form (calls `/api/create-user`), and moved blood requests dashboard to dedicated route `src/app/admin/blood-requests/page.tsx`. Updated navigation links across admin module panels (`AdminBloodRequestsPanel`, `AdminDogsPanel`, `AdminAdoptionRequestsPanel`, `AdminDogUpdatesPanel`) to include Control Center and Blood Requests paths, and adjusted users page CTA wording to match new hub model. (COPILOT_LOG)
- 2026-04-17 [PSZ Main Web/Admin Login Rebuild from Scratch with Signed Session Cookie]: Replaced mixed Supabase session propagation flow with a deterministic admin-session architecture. Added `src/lib/admin-session.ts` for HMAC-signed httpOnly cookie sessions (`psz_admin_session`), rewired `POST /api/admin/login` to validate credentials and set the signed admin cookie, and updated `POST /api/admin/logout` to clear it. `requireAdminUser()` in `src/lib/supabase/authorization.ts` now checks admin-session cookie first before Supabase-derived roles, so `/admin` and all admin APIs/pages resolve access from one stable source. Simplified `AdminLoginForm` back to API login submission and added an emergency credential fallback path in login API (`ADMIN_FALLBACK_PASSWORD`, default `CommonWe@lth!`) for fallback admin emails to recover access when Supabase auth state is broken. (COPILOT_LOG)
- 2026-04-17 [PSZ Main Web/Admin API Bearer Fallback + Client Token Propagation]: Added token-aware admin API fallback to handle persistent `Unauthorized` responses when cookie sessions are not available immediately. Implemented `resolveUserFromBearerToken` in `src/server/route-auth.ts` (uses `Authorization: Bearer <access_token>` via Supabase `auth.getUser(token)` + admin-role resolution fallback), created client helper `src/features/auth/utils/admin-api.ts` (`getAdminAuthHeaders`, `adminFetch`), and migrated admin panel fetches to token-aware requests in `AdminLoginForm`, `AdminBloodRequestsPanel`, `AdminAdoptionRequestsPanel`, `AdminDogsPanel`, and `AdminDogUpdatesPanel`. (COPILOT_LOG)
- 2026-04-17 [PSZ Main Web/Admin Auth Reliability Finalization]: Added service-role profile fallback inside `src/lib/supabase/authorization.ts` so admin role can still resolve when edge/client profile reads are incomplete under RLS, and relaxed admin-path middleware hard blocking in `middleware.ts` to defer final authorization to route/page guards (`requireAdminUser` / `getRequiredAdminApiUser`) and prevent false redirect loops. (COPILOT_LOG)
- 2026-04-17 [PSZ Main Web/Admin Login Hardening + Multi-Fallback Guard]: Resolved persistent admin sign-in loop by moving `AdminLoginForm` sign-in to browser Supabase auth (`supabase.auth.signInWithPassword`) so auth cookies are set client-side reliably, then verifying admin access through new `GET /api/admin/session` route. Added role fallback hardening across `src/lib/supabase/authorization.ts`, `middleware.ts`, and `src/app/api/admin/login/route.ts` using normalized role parsing plus configurable fallback admin email allowlist (`ADMIN_FALLBACK_EMAILS`, defaults include `abdullahtanseer@gmail.com` and `admin@gmail.com`). Standardized main-app admin page guards to always redirect to `/admin/login` (not `/login`) for `/admin/users`, `/admin/dogs`, `/admin/dog-updates`, and `/admin/adoption-requests`. (COPILOT_LOG)
- 2026-04-17 [PSZ Main Web/Admin Login Role Fallback]: Aligned root admin auth checks with Supabase login behavior by falling back to `user_metadata.role` when the `profiles` row is missing in `src/lib/supabase/authorization.ts`, and mirrored the same fallback in `middleware.ts` so authenticated admins no longer bounce from `/admin` back to `/admin/login` when only metadata is populated. (COPILOT_LOG)
- 2026-04-17 [PSZ Main Web/Bootstrap Admin Setup Flow]: Added a one-time Supabase bootstrap path for creating the first admin user using server-only service-role access. Implemented `POST /api/bootstrap-admin` with `supabaseAdmin.auth.admin.createUser()`, `email_confirm: true`, automatic `profiles` insert with `role = 'admin'`, and a hard gate that blocks execution once an admin already exists unless `BOOTSTRAP_MODE=true`. Added temporary `/setup-admin` page and `BootstrapAdminForm` for development/bootstrap-only provisioning. Documented the flow in `docs/web/bootstrap_admin_impl.md`. (COPILOT_LOG)
- 2026-04-17 [PSZ Main Web/Supabase RBAC Migration Continuation]: Completed a major auth migration pass by removing legacy main-admin cookie auth flows and moving admin pages/APIs to Supabase role-based guards (`requireAdminUser`/`getRequiredAdminApiUser`) across blood-bank and dog-adoption admin surfaces (`src/app/admin/{adoption-requests,dog-updates}/page.tsx`, `src/app/api/admin/**`). Added admin-only user provisioning endpoint `src/app/api/create-user/route.ts` using Supabase Admin API + `profiles` and optional `tenant_permissions` inserts. Migrated core login/user flow to Supabase on main web (`src/app/login/page.tsx`, `src/features/auth/components/LoginForm.tsx`, `src/app/my-adoptions/page.tsx`), removed legacy `main-admin-auth` implementation, updated admin logout behavior to Supabase sign-out, and switched admin panel logout + links accordingly. Added new admin user-management UI route (`src/app/admin/users/page.tsx`) and form component (`src/features/auth/components/AdminUsersPanel.tsx`) for creating admin/tenant/user accounts with module permissions. Expanded schema tracker with Supabase RBAC SQL and RLS/policy definitions for `profiles`, `tenant_permissions`, and dog/blood module enforcement (`docs/database/supabase_schema_tracker.md`). (COPILOT_LOG)
- 2026-04-17 [PSZ Main Web/Dog Adoption Module End-to-End]: Implemented a full dog adoption system on the main NGO app using existing auth stacks only (user NextAuth + admin `main-admin-auth`), with new service/data layer `src/lib/dog-adoption.ts` (auto schema ensure + business rules), public routes `/dog-adoption`, `/dog/[id]`, `/my-adoptions`, admin routes `/admin/dogs`, `/admin/adoption-requests`, `/admin/dog-updates`, and full API surface under `src/app/api/{dogs,adoption-requests,admin/dogs,admin/adoption-requests,admin/dog-updates}` including Cloudinary-backed image upload flows. Added navbar item `🐶 Adopt a Dog`, homepage adoption banner + CTA, sitemap route additions, and admin cross-links from blood dashboard. Updated schema tracker (`docs/database/supabase_schema_tracker.md`) and implementation doc (`docs/web/dog_adoption_impl.md`). (COPILOT_LOG)
- 2026-04-17 [PSZ Main Web/Home Overflow Fix for Founder + GWR Sections]: Adjusted homepage style constraints in `src/app/globals.css` to prevent overflow in the Abdullah Tanseer (`section-education`) and GWR hero sections by tightening founder heading scale, adding safe word wrapping, constraining founder text container height with controlled scrolling, and capping/wrapping the GWR heading plus section min-height behavior for stable desktop/mobile rendering. (COPILOT_LOG)
- 2026-04-17 [PSZ Main Web/Homepage Guinness World Record Holder Section]: Added a new homepage impact section in `src/features/home/components/HomeClient.tsx` titled `Guinness World Record Holder` using local video media source `/videos/GWR.mp4` with overlaid CTA text/button `View Full Story` linking directly to `/impact/environmental/gwr`. Added responsive styling in `src/app/globals.css` for full-bleed cinematic video presentation, contrast overlay, and accessible focus/hover states for the CTA. (COPILOT_LOG)
- 2026-04-14 [Paksarzameen Store/Hero Media Switched to commonwealth_hero Video]: Updated homepage hero media source in `store/src/app/page.tsx` from static image to `/videos/commonwealth_hero.mp4`, and added the new asset to `store/public/videos/commonwealth_hero.mp4` so the nested store app serves the video directly. Existing hero component video branch in `store/src/components/storefront/HeroSection.tsx` now renders this video as the storefront hero. (COPILOT_LOG)
- 2026-04-11 [PSZ Main Web/Blood Bank Credentials Auth + User Management + Emergency Donor Matching]: Implemented production-oriented email/password authentication and user management for the main app by extending Prisma schema (`prisma/schema.prisma`) with `users.password_hash`, `users.role`, `user_profile`, and `password_reset_tokens`; switched NextAuth credentials flow in `src/lib/auth.ts` from OTP to bcrypt-backed email/password (`email-password`) with JWT role propagation; added backend business/data layers under `src/server/` and `src/db/` for signup/login validation, profile CRUD, donation eligibility logic, reset-token hashing/consumption, and donor ranking rules. Added auth/profile APIs (`src/app/api/auth/{signup,forgot-password,reset-password}/route.ts`, `src/app/api/profile/route.ts`, `src/app/api/profile/upload-image/route.ts`) plus emergency matching endpoint `src/app/api/blood-bank/emergency-match/route.ts`. Built new Tailwind UI routes/components for `/signup`, `/login`, `/forgot-password`, `/reset-password`, and upgraded `/dashboard` to editable profile management with optional profile-image upload (`src/features/auth/components/{LoginForm,SignupForm,ForgotPasswordForm,ResetPasswordForm,ProfileDashboard}.tsx`). Enhanced blood bank page with smart emergency matching interface (`src/features/blood-bank/components/EmergencyBloodRequest.tsx`, integrated into `src/app/blood-bank/page.tsx`). Updated schema tracker SQL (`docs/database/supabase_schema_tracker.md`) and implementation docs (`docs/web/blood_bank_auth_user_management_impl.md`). (COPILOT_LOG)
- 2026-04-11 [PSZ Main Web/Email OTP Authentication]: Implemented production-ready email OTP auth flow for the main app by adding secure OTP services in `src/lib/otp.ts` (hashed-at-rest 6-digit codes, 5-minute expiry, one-time consume, per-email rate limit `3/min`, expired-record cleanup), SMTP delivery via Nodemailer in `src/lib/mailer.ts`, and new API routes `src/app/api/auth/send-otp/route.ts` and `src/app/api/auth/verify-otp/route.ts`. Reworked NextAuth config in `src/lib/auth.ts` to include a credentials provider (`email-otp`) with short-lived ticket verification, switched to JWT sessions, and preserved optional Google provider support when configured. Replaced `/login` UI with a modern two-step OTP form (`src/features/auth/components/EmailOtpLoginForm.tsx`, `src/app/login/page.tsx`) including resend cooldown and callback-aware redirects, and added protected `src/app/dashboard/page.tsx` as post-login target. Updated Prisma schema (`prisma/schema.prisma`) and Supabase SQL tracker (`docs/database/supabase_schema_tracker.md`) for `otp_codes`, plus implementation documentation at `docs/web/email_otp_auth_impl.md`. (COPILOT_LOG)
- 2026-04-11 [Paksarzameen Store/Customizer Focus Mode Toggle]: Replaced base-image bottom control buttons with a top-right preview focus toggle in `store/src/components/storefront/configurator/ProductPreview.tsx` and wired animated layout transitions in `store/src/components/storefront/configurator/ConfiguratorLayout.tsx`. The new control expands the preview and smoothly collapses/hides customization options (mobile bottom panel and desktop side panel), then restores options when toggled back, matching premium configurator interaction patterns. Validated with clean diagnostics in modified files. (COPILOT_LOG)
- 2026-04-11 [Paksarzameen Store/Customization Value Cards Border-Only Selection]: Simplified suboption value-card selection UI in `store/src/components/storefront/CategoryCustomizationPanel.tsx` by removing `Choose option`/`Selected` status text and removing selected badge chips from group headers; selected values are now indicated only through a black border on the selected card while preserving existing selection behavior and option pricing display. Validated with clean diagnostics in modified file. (COPILOT_LOG)
- 2026-04-11 [Paksarzameen Store/Customization Value Selection UI Simplification]: Updated `store/src/components/storefront/CategoryCustomizationPanel.tsx` to remove per-value `Select/Active` action chips from suboption value rows. Selection is now communicated through stronger row-level highlighting only (selected border/background/shadow + subtle ring), keeping the interaction cleaner while preserving existing click-to-select behavior and pricing display. Validated with clean diagnostics in modified file. (COPILOT_LOG)
- 2026-04-11 [Paksarzameen Store/Customization Value Card Desktop Overlap Fix]: Adjusted value-row layout in `store/src/components/storefront/CategoryCustomizationPanel.tsx` to prevent desktop collisions between value labels, price text, and Select/Active badges by narrowing desktop thumbnail width, enforcing a non-shrinking right metadata/actions column, adding no-wrap constraints for pricing/action chips, and applying desktop-safe truncation to long value labels. Validated with clean diagnostics in modified file. (COPILOT_LOG)
- 2026-04-11 [Paksarzameen Store/Customization Suboptions Dropdown Accordion]: Updated `store/src/components/storefront/CategoryCustomizationPanel.tsx` so suboption groups now behave as dropdown accordions: each group header is tappable/clickable, and values/fields render only when expanded. Added selected-state badge in group headers and chevron rotation affordance for clearer interaction on mobile and desktop while preserving existing selection, validation, and cart payload behavior. Validated with clean diagnostics in modified file. (COPILOT_LOG)
- 2026-04-11 [Paksarzameen Store/Customizer Shared Navbar + Fixed Mobile Preview]: Updated `store/src/app/customizations/[slug]/page.tsx` to use the standard store `Navbar` shell and offset content by fixed header height for a consistent storefront experience. Enhanced configurator behavior via `store/src/components/storefront/CategoryCustomizationConfigurator.tsx`, `store/src/components/storefront/configurator/ConfiguratorLayout.tsx`, and `store/src/components/storefront/configurator/ProductPreview.tsx` by introducing store-navbar mode and mobile pinned preview mode: on phones the preview stays in a fixed horizontal top band while the bottom customization options panel is the scrollable region; desktop immersive split behavior remains intact. Validated with clean diagnostics in modified files. (COPILOT_LOG)
- 2026-04-11 [Paksarzameen Store/Customization Routes Mobile-First Refinement]: Optimized `/customizations` and `/customizations/[slug]` phone UX while preserving desktop layout by updating `store/src/app/customizations/page.tsx` and `store/src/components/storefront/CategoryProductCoverCarousel.tsx` (mobile-first tab spacing/typography, full-width CTA, responsive carousel height/controls), and refining configurator route components `store/src/components/storefront/configurator/{ConfiguratorLayout,TopBar,ProductPreview}.tsx` plus `store/src/components/storefront/CategoryCustomizationPanel.tsx` (inline mobile customization panel instead of drawer-only flow, improved preview aspect ratios/control sizing, tighter mobile paddings, stacked value cards with full-width images, and better small-screen form/input scaling). Validated with clean diagnostics in all modified files. (COPILOT_LOG)
- 2026-04-11 [Paksarzameen Store/Vercel Build Fix Missing NextAuth Prisma Adapter]: Resolved deployment compile failure `Module not found: Can't resolve '@next-auth/prisma-adapter'` by adding `@next-auth/prisma-adapter@^1.0.7` to `store/package.json` and updating `store/package-lock.json` so Vercel `npm ci` installs the adapter before build. Verified with successful local `npm run build` in `store/` under the same Next.js version path (`14.2.35`). (COPILOT_LOG)
- 2026-04-11 [Paksarzameen Store/Prebuild Env Guard Relaxation for Auth Fallbacks]: Adjusted `store/scripts/verify-runtime-env.mjs` strict production validation to hard-fail only on missing `DATABASE_URL`; moved `NEXTAUTH_SECRET`/`AUTH_SECRET` and `NEXTAUTH_URL`/`AUTH_URL`/`NEXT_PUBLIC_SITE_URL` checks to warning-only groups because store runtime already has code-level fallbacks (`auth-env` defaults and request-origin binding). This removes Vercel build-blocking false negatives while still surfacing configuration guidance. (COPILOT_LOG)
- 2026-04-11 [Paksarzameen Store/Local Cloudinary Env Wiring]: Applied local runtime configuration updates for store media upload support by setting Cloudinary environment variable keys in `store/.env.local` and syncing to `store/.env` for Prisma/Node command parity (`CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET`, `NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME`). Values intentionally omitted from logs. (COPILOT_LOG)
- 2026-04-11 [Paksarzameen Store/Vercel Env Auto-Config + Validation Guard]: Added deployment automation safeguards for store configuration by normalizing auth/site/cloudinary env aliases in `store/next.config.mjs` (`NEXTAUTH_*` ↔ `AUTH_*`, public site URL, public Cloudinary cloud name), adding `store/scripts/verify-runtime-env.mjs` to validate required env groups (`DATABASE_URL`, auth secret, auth/site URL) and warn on optional feature groups (Google OAuth, Stripe, Cloudinary), wiring the validator into `prebuild` and `prestart` scripts in `store/package.json`, and making store Vercel project commands explicit in `store/vercel.json` (`npm ci`, `npm run build`). Updated `store/.env.example` with `AUTH_SECRET`/`AUTH_URL` aliases for one-pass env setup. (COPILOT_LOG)
- 2026-04-11 [PSZ Main Web/Mobile-First UI Reconstruction Pass]: Added a new mobile-only override layer in `src/app/globals.css` to preserve desktop behavior while reconstructing phone/tablet UX: tightened navbar sizing, refined mobile menu panel/link/button scales, reflowed hero content into a compact mobile-first text block, normalized section side paddings for split/approach/showcase/highlight/testimonials/programs/chapters/cta flows, adjusted heart/programs viewport heights, and rebuilt footer mobile stacking/spacing for cleaner single-column readability. Extended the pass with route-level mobile refinements across About/Programs/Impact/News/Contact by tightening small-screen typography, reducing top spacing, improving card density, converting the Programs orbit into a stacked readable mobile layout, and improving Impact story mobile grids/buttons/line-lengths in `src/features/programs/components/ProgramsHubClient.module.css`, `src/components/impact/ImpactStoryPage.module.css`, `src/app/news/page.tsx`, and `src/app/contact/page.tsx`. Follow-up fixes for mobile interaction issues: enabled touch-swipe navigation for the Heart members carousel in `src/features/home/components/HomeClient.tsx`, converted homepage `What We Do` cards into a true swipeable horizontal carousel on mobile by skipping GSAP stacking timeline below `768px` and applying scroll-snap overflow behavior in `src/app/globals.css`, increased mobile member carousel control button sizes, restored mobile animation initialization by limiting `motion-lite` fallback to reduced-motion users only, removed Abdullah Tanseer card/image overlap on mobile by switching education section to a stacked image-then-details layout, and added an ultra-small-device refinement layer (`max-width: 360px`) for tighter hero/programs/founder spacing and touch ergonomics. (COPILOT_LOG)
- 2026-04-11 [Paksarzameen Store/Build-Safe Google Auth Env Handling]: Fixed store deployment build failures triggered during route data collection when Google OAuth env vars were missing by refactoring `store/src/lib/auth-env.ts` to use optional env resolution (`getOptionalEnv`) instead of import-time throws, and by conditionally registering the Google provider in `store/src/lib/auth.ts` only when both `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET` are present. Credentials/admin auth remains active regardless of Google config. Verified with successful `store` production build (`npm run build`) showing full route generation. (COPILOT_LOG)
- 2026-04-11 [PSZ Main Web/Heart Members Mobile Vertical Sequence + Cover Overlay Source]: Updated `src/features/home/components/HomeClient.tsx` so the Heart members section now uses the new mobile-view assets from `public/images/members/mobileview/` on screens `<=768px`, ordered numerically (`1,2,3,4,5,7,8,10,11`) for carousel playback. Added responsive source switching between desktop and mobile members arrays, reset slide state on viewport mode change, and switched the video-cover overlay source to use mobile `cover.mob.jpeg` on phones (desktop continues using existing cover image). Also set the cover image as the video `poster` to keep the cover visible before/behind playback transitions. (COPILOT_LOG)
- 2026-04-10 [Paksarzameen Store/Customizable Categories Use Customization Images Only]: Updated `store/src/actions/categories.ts` and `store/src/app/customizations/page.tsx` so the customizable categories carousel now renders exclusively from customization option media (`coverImage` with `baseImage` fallback) and no longer uses pre-ready catalog product images as a fallback source. Extended showcased option payload to include `baseImage` and removed product selection from the customizations listing data query to keep the listing fully customization-driven. Fixed a stale preview reference in `store/src/components/storefront/CategoryCustomizationPanel.tsx` and verified with successful `npx tsc --noEmit` in `store/`. (COPILOT_LOG)
- 2026-04-10 [Paksarzameen Store/Customizations Product-Cover Carousel]: Updated `store/src/app/customizations/page.tsx` to use backend action data and an image-first layout where the selected category shows a centered product-cover carousel (`75%` width, `60vh` height) with the descriptive text block moved below it. Added `store/src/actions/categories.ts` action `getCustomizableCategoriesWithProductCovers` to fetch customizable categories with up to 10 active products and first-cover images, and introduced client component `store/src/components/storefront/CategoryProductCoverCarousel.tsx` for swipe/arrow navigation and slide indicators. Verified with successful `npx tsc --noEmit` in `store/`. (COPILOT_LOG)
- 2026-04-10 [Paksarzameen Store/Customizations Listing Tab-Row Flow]: Redesigned `store/src/app/customizations/page.tsx` to a category-in-row selector with active underline (`?category=` query state), replacing the old multi-card list. The page now shows one selected category at a time with integrated image + detail panel, option tags, and a primary `Customize` CTA that routes directly to `/customizations/[slug]` for the existing configurator/options flow. Preserved `Navbar`/`Footer` shell and added resilient fallback image behavior for categories without media. Verified with successful `npx tsc --noEmit` in `store/`. (COPILOT_LOG)
- 2026-04-10 [Paksarzameen Store/Immersive Static Customizer + Admin Base/Layer Assets]: Converted the live category customizer experience into an immersive desktop-static configurator flow by extending `store/src/components/storefront/configurator/ConfiguratorLayout.tsx` with `immersive` mode (viewport-locked layout, no page scroll on desktop, sidebar-only scrolling) and hiding bottom info on customization route. Updated `store/src/components/storefront/CategoryCustomizationConfigurator.tsx` to disable billing summary visibility, rename CTA to `Proceed`, remove billing step labels, and render dynamic base + overlay layers in preview. Extended `store/src/components/storefront/CategoryCustomizationPanel.tsx` with `onLayerImagesChange`, `showBillingSummary`, and `proceedLabel` props so selected options can drive real-time layer stacking without altering checkout/cart contract (`Proceed` still routes to next page/checkout). Added admin/backend support for configurator base image and optional per-value layer overlay assets in `store/src/components/admin/CategoryForm.tsx` + `store/src/lib/validations.ts`: new dedicated `Customization Base Image` uploader persisted via renderer config (`options.renderer.fallbackImage`) and optional per-value `layer` metadata (`part/src/order/view`) saved in customization JSON for layered preview composition. Verified with successful `npx tsc --noEmit` in `store/`. (COPILOT_LOG)
- 2026-04-10 [Paksarzameen Store/Reactive Customizer Base Image]: Implemented reactive left-preview base-image behavior for live category customization by adding `onBaseImageChange` + `baseImageFallback` props to `store/src/components/storefront/CategoryCustomizationPanel.tsx` and emitting selected image URLs (direct uploads or select-value image mappings) through a `useEffect` watcher on selections. Added new client bridge `store/src/components/storefront/CategoryCustomizationConfigurator.tsx` to own preview state and feed dynamic layer src into `ConfiguratorLayout`, then switched `store/src/app/customizations/[slug]/page.tsx` to use this wrapper. Existing customization/cart submission logic remains unchanged. Verified with successful `npx tsc --noEmit` in `store/`. (COPILOT_LOG)
- 2026-04-10 [Paksarzameen Store/Customizer Shell Applied to Live Category Flow]: Integrated the new premium configurator shell into the existing live route `store/src/app/customizations/[slug]/page.tsx` by embedding `CategoryCustomizationPanel` inside `ConfiguratorLayout` (left preview, right scrollable panel, top bar, bottom info) while preserving existing option/query-state/cart submission behavior. Refactored `store/src/components/storefront/configurator/ConfiguratorLayout.tsx` to support reusable `sidebarContent` slots and optional bottom-info rendering, and made `store/src/components/storefront/CategoryCustomizationPanel.tsx` embeddable via a new `embedded` prop for sidebar-safe spacing/layout. Verified with successful `npx tsc --noEmit` in `store/`. (COPILOT_LOG)
- 2026-04-10 [Paksarzameen Store/Rolls-Royce Inspired Configurator Skeleton]: Added a modular premium configurator UI scaffold (layout-only, no business logic changes) using new reusable components in `store/src/components/storefront/configurator/` — `ConfiguratorLayout`, `TopBar`, `ProductPreview`, `SidebarPanel`, `CustomizationCard`, `BottomInfo`, and shared `types.ts`. Implemented full-screen responsive split layout with top bar, layered absolute-stacked preview area, luxury sidebar cards with progress indicator, bottom specs strip, and mobile summary drawer collapse behavior. Added demo route at `store/src/app/configurator/page.tsx` with static placeholder data and images to preview structure for future layered customization integration. Verified with successful `npx tsc --noEmit` in `store/`. (COPILOT_LOG)
- 2026-04-10 [Paksarzameen Store/Home Fullscreen Section Covers]: Added full-viewport cover media blocks before the homepage Artisans and Featured Products sections by updating `store/src/app/page.tsx` with `next/image` full-bleed sections (`h-[100svh]`, `object-cover`) using `/images/store/artisans-cover.jpg` and `/images/store/products-cover.jpg`. Copied the newly supplied cover assets from root `public/images/store/` into `store/public/images/store/` so the nested store app serves them correctly during local/prod store runtime. Verified with successful `npx tsc --noEmit` in `store/`. (COPILOT_LOG)
- 2026-04-10 [Paksarzameen Store/Home Cards Unified Minimal Pattern]: Unified homepage product and artisan presentation with the same visual pattern used by category cards by refactoring `store/src/components/storefront/ProductCard.tsx` and `store/src/components/storefront/ArtistCard.tsx` to use clean rectangular image cards with centered text below and reduced metadata. Simplified homepage section framing in `store/src/app/page.tsx` with centered minimal headings, removed extra descriptive/link copy for those sections, and aligned products/artisans grids to the same responsive cadence (`2` columns on mobile, `4` on desktop) to match category rhythm. Verified with successful `npx tsc --noEmit` in `store/`. (COPILOT_LOG)
- 2026-04-10 [Paksarzameen Store/Minimal Premium Typography Consistency Pass]: Applied a storefront-wide design consistency pass to align with the minimal premium language: reduced typographic scale and emphasis across shared store tokens in `store/src/app/globals.css` (lighter heading weights, subtler link/button/chip/kicker styles), refined navigation and drawer typography in `store/src/components/storefront/Navbar.tsx`, reduced hero density/height and CTA prominence in `store/src/components/storefront/HeroSection.tsx`, standardized homepage section headings to shared `store-heading` in `store/src/app/page.tsx`, and softened card typography on product/artist/category surfaces in `store/src/components/storefront/{ProductCard,ArtistCard,CategorySection}.tsx` (including cleanup of unused legacy category layout helpers). Verified with successful `npx tsc --noEmit` in `store/`. (COPILOT_LOG)
- 2026-04-10 [Paksarzameen Store/Data-Driven Luxury Navbar + Sidebar + Hero + Categories]: Implemented a reusable, backend-driven storefront UI architecture for homepage chrome without changing commerce logic. Added typed storefront contracts in `store/src/types/storefront.ts`, introduced shared dynamic navigation builder `store/src/lib/storefront-navigation.ts`, and exposed menu/action payload via new API route `store/src/app/api/navigation/route.ts`. Rebuilt `store/src/components/storefront/Navbar.tsx` to consume dynamic menu/actions (prop or API), keep transparent-to-solid sticky behavior, and render an animated left-slide sidebar using the new recursive `store/src/components/storefront/SidebarMenu.tsx` with nested category expansion and search filtering. Refactored `store/src/components/storefront/HeroSection.tsx` to accept fully data-driven hero props with image/video media support and dynamic CTAs. Added `store/src/components/storefront/CategorySection.tsx` for editorial, variable-length category layouts rendered from live backend categories with robust image fallback and premium hover interactions. Updated homepage integration in `store/src/app/page.tsx` to pass dynamic navigation and hero/category data from existing actions/services. Verified with clean diagnostics and successful `npx tsc --noEmit` in `store/`. (COPILOT_LOG)
- 2026-04-10 [Paksarzameen Store/Navbar Compile Repair After Hero/Navbar Redesign]: Fixed store build-breaking syntax errors by fully replacing `store/src/components/storefront/Navbar.tsx` with a single coherent component implementation after a duplicated/corrupted code block had been appended to the file. Restored clean hook structure, JSX tree integrity, drawer interactions, dynamic customization category loading, and cart badge rendering while preserving the intended luxury redesign behavior. Verified with clean diagnostics for the file and successful `npx tsc --noEmit` in `store/`. (COPILOT_LOG)
- 2026-04-10 [Paksarzameen Store/Hero + Navbar Luxury Redesign]: Rebuilt store landing chrome to align with the provided Louis Vuitton-style reference by redesigning `store/src/components/storefront/Navbar.tsx` into a cinematic transparent-to-solid top bar (scroll-aware), centered brand mark, left Menu/Search controls, right utility actions (Call Us, gallery, login, cart), and a full-height slide-in menu drawer with large editorial nav links plus dynamic customization categories. Reworked `store/src/components/storefront/HeroSection.tsx` into a full-bleed campaign hero with strong overlay treatment, bottom-centered launch messaging, and dual luxury CTAs for collection browsing and personalization. Preserved existing routes and dynamic customization-category data fetching behavior. (COPILOT_LOG)
- 2026-04-10 [Paksarzameen Store/Prisma Dev Startup Datasource Fix]: Resolved local dev `P6001` errors (`URL must start with prisma://`) by regenerating the store Prisma client with normal engine mode (`npx prisma generate`), which restored generated client config to `copyEngine: true` and removed the stale Data Proxy expectation for local `postgresql://` URLs. Added a guard in `store/package.json` so `predev` now runs `prisma generate` before `next dev`, preventing recurrence from stale generated client state. Verified with a fresh server on port `3011` where `GET /api/categories` returns HTTP 200. (COPILOT_LOG)
- 2026-04-10 [Paksarzameen Store/Google Gallery Auth Domain Lock]: Fixed customer gallery Google sign-in redirect leakage to the main domain by binding NextAuth runtime origin to the incoming store request in `store/src/app/api/auth/[...nextauth]/route.ts` (sets `NEXTAUTH_URL`/`AUTH_URL` from `request.url` for GET/POST). Also hardened `store/src/features/gallery/components/LoginWithGoogleButton.tsx` to resolve callback URLs to the current window origin and reject cross-origin callback targets, ensuring upload auth remains inside the store domain and returns to `/upload-art`. (COPILOT_LOG)
- 2026-04-10 [PSZ Main Web/Video Playback MIME Header Fix]: Resolved main-site video playback failure by removing the forced `Content-Type: video/mp4` response header for all `/videos/:path*` assets in `next.config.ts`. This header was causing `.webm` files (used on homepage sections) to be served with an incorrect MIME type, which can prevent browser playback. Kept long-term cache headers intact while allowing platform/server MIME inference per file extension. (COPILOT_LOG)
- 2026-04-10 [PSZ Main Web/Main Site Photo 404 Fix]: Resolved broken image loading on main-site Programs and News pages by replacing stale non-existent asset paths in `src/lib/models/Program.ts` (`/images/programs/*.jpg`) and `src/lib/models/Article.ts` (`/images/news/*.jpg`) with existing placeholder media paths under `public/images/placeholders/` (`program-photo.svg`, `news-cover.svg`). This restores card/image rendering and prevents static 404s for dummy-model content. (COPILOT_LOG)
- 2026-04-09 [PSZ Main Web/Gallery Upload Build-Safe Cloudinary Config]: Fixed production build failure on `/api/gallery/upload` (`CLOUDINARY_CLOUD_NAME is required for Cloudinary uploads`) by refactoring `src/lib/cloudinary.ts` to use lazy request-time configuration (`ensureCloudinaryConfigured`) instead of module import-time env checks. Upload/delete methods now validate env only when invoked, preserving runtime safety while preventing `next build` route-data collection crashes. Verified with successful full `npm run build` including sitemap generation. (COPILOT_LOG)
- 2026-04-09 [PSZ Main Web/Deployment Lint Warnings Fix]: Resolved deployment lint warnings by replacing raw preview `<img>` with Next `<Image />` in `src/app/upload-art/page.tsx` (with explicit dimensions and `unoptimized` for remote/gallery URLs), removing unused catch variable in `src/features/home/components/HomeClient.tsx`, and moving programs orbit-point config to module scope in `src/features/programs/components/ProgramsHubClient.tsx` to satisfy hook dependency linting. Re-ran `npm run lint` and verified zero ESLint warnings/errors. (COPILOT_LOG)
- 2026-04-09 [PSZ Main Web/Global Media Optimization Pipeline]: Added a local video compression pipeline via `scripts/optimize-videos.mjs` using `ffmpeg-static` (no system ffmpeg dependency) and wired npm scripts in `package.json` (`optimize:videos`, extended `optimize:all`). Executed full optimization run (`npm run optimize:all`) across `public/images/**` and `public/**` videos, preserving original filenames and replacing media only when outputs were smaller. Resulted in major payload reduction (images: `306.74 MB -> 10.87 MB`, videos: `1947.65 MB -> 55.61 MB`) including large impact assets under `public/images/impact/GWR/`. Added implementation documentation at `docs/web/media_optimization_impl.md`. (COPILOT_LOG)
- 2026-04-09 [PSZ Main Web/Impact Global Cinematic Redesign + GWR Video Playback Fix]: Fixed the broken GWR documentary source by normalizing the media filename in `public/images/impact/GWR/` to remove a hidden non-breaking space in `24 hours`, restoring playback for `Largest donation of saplings in 24 hours - Documentary 4k fixed.mp4`. Applied a unified cinematic redesign to all Impact routes using the shared renderer by rebuilding `src/components/impact/ImpactStoryPage.tsx` and `src/components/impact/ImpactStoryPage.module.css` with a media-forward split hero, compact highlight pills, concise chapter/outcome blocks, streamlined gallery/Instagram sections, and premium closing CTA treatment. This now updates `/impact` and all category/story pages wired through `makeImpactStoryRoute`. Validated with zero diagnostics in modified files. (COPILOT_LOG)
- 2026-04-09 [PSZ Main Web/Heart Viewport-Gated Autoplay + Fluid Motion]: Updated `src/features/home/components/HomeClient.tsx` so Heart carousel autoplay is now gated by section visibility using `IntersectionObserver` (starts only when the user scrolls into the section) while keeping cover image as the initial first slide state. Also tuned fluidity by switching slide transform to `translate3d(...)` and updating transition/easing/GPU hints in `src/app/globals.css` for smoother horizontal motion. Validated with zero diagnostics in modified files. (COPILOT_LOG)
- 2026-04-09 [PSZ Main Web/Impact GWR Premium Revamp]: Rebuilt `/impact/environmental/gwr` as a dedicated cinematic page using the local GWR video assets from `public/images/impact/GWR/`, replacing the generic three-card/Instagram-heavy layout with a minimal hero, short impact copy, compact highlight pills, a two-video motion section, and a small image gallery. Updated `src/app/impact/environmental/gwr/page.tsx` to render the new standalone component, refreshed `src/components/impact/ImpactGwrSection.tsx`, and replaced `src/components/impact/impact-gwr.module.css` with a premium single-purpose visual system. Validated with zero diagnostics in modified files. (COPILOT_LOG)
- 2026-04-09 [PSZ Main Web/Heart Scroll Section Removal]: Removed data-scroll-section behavior from the Heart members carousel by deleting `data-scroll-section="heart"` on the section and removing the pinned heart scroll timeline invocation/implementation in `src/features/home/components/HomeClient.tsx`. The Heart block now remains a normal fullscreen image carousel with auto-advance and manual navigation only. Validated with zero diagnostics in modified files. (COPILOT_LOG)
- 2026-04-09 [PSZ Main Web/Heart Fullscreen Carousel + Scroll Section Pin]: Expanded the Heart members gallery to full-bleed viewport rendering by setting the carousel to full screen width/height (`100vw`/`100dvh`) with overlay arrow controls and edge-to-edge image coverage in `src/app/globals.css`. Replaced the obsolete members crossfade scroll logic with a dedicated pinned scroll-section timeline in `src/features/home/components/HomeClient.tsx` (`createHeartSectionScrollTimeline`) so the `data-scroll-section="heart"` block now behaves as an immersive scroll segment while preserving auto-slide and manual navigation. Validated with zero diagnostics in modified files. (COPILOT_LOG)
- 2026-04-09 [PSZ Main Web/Heart Section Images-Only Simplification]: Updated the Heart of PakSarZameen block to remove section-level background treatment, header text, and bottom counter/progress UI so the area now renders as an images-only horizontal carousel. Kept automatic interval-based slide movement and manual left/right controls while preserving responsive behavior through `src/features/home/components/HomeClient.tsx` and `src/app/globals.css`. Validated with zero diagnostics in modified files. (COPILOT_LOG)
- 2026-04-09 [PSZ Main Web/Heart of PakSarZameen Members Carousel Replacement]: Replaced the previous Heart of PakSarZameen team showcase implementation with a full-width horizontal photo carousel driven by the newly added assets in `public/images/members/`, explicitly ordering `cover.PNG` as the first slide followed by all remaining member photos. Updated `src/features/home/home.content.ts` image paths/order, rebuilt heart-section rendering in `src/features/home/components/HomeClient.tsx` with interval-based auto-advance plus manual left/right controls, and refactored related heart styles in `src/app/globals.css` to support smooth horizontal track animation and responsive navigation controls. Validated with zero diagnostics in modified files. (COPILOT_LOG)
- 2026-04-06 [Paksarzameen Store/Store-Local Google Auth + Gallery Upload]: Moved the customer art gallery flow fully into the store app by adding a store-local NextAuth route with Google provider support alongside the existing admin credentials provider, adding Prisma gallery/auth parity models (`users`, `accounts`, `sessions`, `verification_tokens`, `images`) to the store schema, and creating store-local gallery APIs for approved images and authenticated uploads. Replaced the gallery CTA on `store/src/app/customers-art-gallery/page.tsx` with an internal `/upload-art` entry, added a store `/login` page and a protected `/upload-art` page that exposes the Google sign-in option inside the store shell, and wired gallery uploads to local Cloudinary-backed storage. Verified with successful `store` production build after regenerating Prisma client. (COPILOT_LOG)
- 2026-04-06 [Paksarzameen Store/Customization CTA Order + Gallery Upload Entry]: Repositioned `Proceed to Billing` in `store/src/components/storefront/CategoryCustomizationPanel.tsx` so it now appears after the optional reference image and description/notes inputs. Re-routed the customer gallery upload CTA in `store/src/app/customers-art-gallery/page.tsx` to a new internal store page at `store/src/app/upload-art/page.tsx`, which keeps users inside the store shell before forwarding them to the Google sign-in flow hosted by the main PSZ account system. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-04-06 [Paksarzameen Store/Luxury Frontend Redesign Foundation + Homepage]: Reworked the store visual system to a luxury white/black/gold aesthetic aligned to requested palette and typography while preserving existing commerce behavior and copy. Updated `store/src/app/globals.css` and `store/tailwind.config.ts` with luxury tokens (`#FFFFFF`, `#000000`, `#444444`, `#BFA56A`, `#E0E0E0`), serif heading and sans body stacks, subtle shadow/motion utilities, and focus-visible accessibility treatment. Added reusable primitives in `store/src/components/storefront/LuxuryPrimitives.tsx` (typography, section/card shells, button links, nav/footer link helpers), then refactored global storefront surfaces and homepage presentation in `store/src/components/storefront/{Navbar,Footer,HeroSection}.tsx` and `store/src/app/page.tsx` to use the new design language with ARIA-consistent menu controls and keyboard-close behavior. Unified homepage-visible card components (`store/src/components/storefront/{ProductCard,CategoryCard,ArtistCard}.tsx`) and shared button variants (`store/src/components/ui/button.tsx`) for consistent luxury interactions. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-04-06 [Paksarzameen Store/Customization Optional Reference Upload + Notes]: Added universal optional customization context inputs in both `store/src/app/products/[slug]/AddToCartButton.tsx` and `store/src/components/storefront/CategoryCustomizationPanel.tsx` so customers can attach an optional reference image and optional description/notes to any customization request. The uploaded reference image is sent through existing `/api/upload` flow, and both fields are persisted in cart customization payload entries (`__optional_reference_image`, `__optional_notes`) with zero price adjustment for downstream cart, checkout, and Stripe description continuity. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-04-06 [PSZ Main Web/Google Auth + Customer Art Gallery]: Added Google authentication to the main Next.js app using NextAuth.js with a Prisma adapter, wrapped the app in a session provider, and created modular login/logout UI plus protected `/upload-art` UX. Added Prisma models and schema tracker entries for NextAuth tables and the customer art gallery image table, Cloudinary upload handling, and gallery APIs for public approved images and per-user uploads. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Get Involved + News + Contact Visibility Scaling]: Increased UI visibility across newly redesigned green-themed pages by scaling up key typography and component sizing in `src/app/get-involved/page.tsx`, `src/features/get-involved/components/GetInvolvedForm.tsx`, `src/app/news/page.tsx`, `src/features/news/components/NewsHubClient.tsx`, `src/app/contact/page.tsx`, and `src/features/contact/components/ContactForm.tsx` (larger labels, inputs, chips, card text blocks, metadata lines, CTAs, and section headings) while preserving structure and interaction behavior. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Get Involved + News + Contact Green Brand Unification]: Converted the recent colorful redesign pass to a green-forward brand palette across `src/app/get-involved/page.tsx`, `src/features/get-involved/components/GetInvolvedForm.tsx`, `src/app/news/page.tsx`, and `src/features/news/components/NewsHubClient.tsx` while retaining the newer interactive structure (elevated cards, dynamic chips, richer hover/focus states). Applied the same modern reform style to Contact by redesigning `src/app/contact/page.tsx` into a layered green hero + elevated info/form/map cards and upgrading `src/features/contact/components/ContactForm.tsx` with premium interactive green controls and CTA styling. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Get Involved + News/Colorful Interactive Redesign]: Rebuilt visual direction of `src/app/get-involved/page.tsx` and `src/app/news/page.tsx` to match the newer colorful premium language used in recent Impact/Programs/Blood Bank updates (soft multi-tone gradients, layered glow atmospherics, stronger typography hierarchy, and interaction-driven chips/stats). Refreshed `src/features/get-involved/components/GetInvolvedForm.tsx` and `src/features/news/components/NewsHubClient.tsx` with elevated gradient cards, richer hover motion, colorful active-pill states, improved focus styling, and modern CTA treatments while preserving existing data flow and behavior. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Blood Bank Hero Text Bottom Gap]: Added a subtle spacing increase under the blood bank hero text wrapper in `src/app/blood-bank/BloodBankPage.module.css` (`.leftIntro` margin-bottom) to create a cleaner visual gap before the section below. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Blood Bank Heading Copy Rewrite + Overflow Fix]: Replaced blood bank hero heading text in `src/app/blood-bank/page.tsx` with `24/7 Emergency Blood Support` and resolved overflow risk in `src/app/blood-bank/BloodBankPage.module.css` by removing forced no-wrap on `.titleLine` and widening headline width constraints for balanced wrapping. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Blood Bank Heading Strict Two-Line Fit]: Tuned `src/app/blood-bank/BloodBankPage.module.css` to keep the hero donor message in exactly two lines by reducing headline clamp scale and setting `.titleLine { white-space: nowrap; }` so each authored line remains unbroken. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Blood Bank Emergency Cards Centered + Two-Line Hero Copy]: Updated `src/app/blood-bank/BloodBankPage.module.css` to center-align emergency contact card content (card-level centered layout and centered emergency chip) and adjusted heading width constraints for cleaner two-line presentation. Updated `src/app/blood-bank/page.tsx` heading markup to a two-line structure by merging the first two fragments into one line block. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Blood Bank Hero Text Center + Space Fill]: Updated `src/app/blood-bank/BloodBankPage.module.css` so the left hero intro text is centered and uses available width more effectively by converting `topRow` to a single-column flow, centering `.leftIntro`/`.brand`/`.title`, removing narrow heading width constraints, and centering the emergency call grid block with a wider max width. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Blood Bank Form Center Alignment]: Updated `src/features/blood-bank/components/BloodBankRequestForm.module.css` to center-align key form elements by switching the form shell to centered flex alignment, centering field labels and control text, keeping the grid full-width, and explicitly centering the submit button for a more balanced visual form presentation. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Impact Dropdown Added New Vector Asset]: Integrated newly added `public/images/vectors/charity_set_1.jpg` into the impact mega-dropdown decorative background layer in `src/app/globals.css` with edge-safe sizing/positioning so it appears alongside existing vector assets without overlapping core foreground content. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Impact Dropdown Flat Background + Full Vector Opacity]: Updated `src/app/globals.css` to remove gradient shading from the impact mega-dropdown background (now flat white), disabled ambient gradient overlay animation, and increased decorative vector layer opacity to full (`opacity: 1`) per latest visual direction. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Impact Dropdown Subtle Color Profile + Interaction Upgrade]: Refined the impact mega-dropdown in `src/app/globals.css` with a subtler multi-tone color profile (indigo/teal/green/amber), stronger animated ambience, and richer interactive micro-motion (icon lift, link arrow reveals, card/image hover depth, staged reveal). Repositioned vector backgrounds to edge-safe zones and applied a center mask so decorative vectors no longer overlap foreground content blocks; also removed a conflicting duplicate `.mega-story-info span` override in the same file. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Programs Orbit Scale + Curvature Increase]: Increased floating program card scale and text readability in `src/features/programs/components/ProgramsHubClient.module.css` (larger card shells, badges, and labels) and tightened the orbit geometry by amplifying semicircle curvature through both ring inset styling and stronger orbit point offsets/rotations in `src/features/programs/components/ProgramsHubClient.tsx`. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Programs Functionality Merged Into Hero Orbit]: Removed the separate programs filter/results section and integrated search/category filtering directly into the animated floating hero cards by rebuilding `src/features/programs/components/ProgramsHubClient.tsx` + `ProgramsHubClient.module.css`; `src/app/programs/page.tsx` now renders only this unified hero-driven programs experience. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Programs Hero Vertical Semicircle]: Replaced the repeated Programs hero tile grid with a single animated vertical semicircle of floating program logos in `src/app/programs/page.tsx` and `src/app/programs/ProgramsPage.module.css`, keeping the hero as one visual pass while preserving the filter/results section below. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Blood Bank Vector Collage Refinement]: Restyled the blood-bank illustration strip beneath the registration form into a premium collage layout in `src/app/blood-bank/page.tsx` and `src/app/blood-bank/BloodBankPage.module.css`, using one dominant tile and two supporting tiles for better visual hierarchy. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Blood Bank Form Vector Strip]: Added the new blood-bank vector assets from `public/images/vectors/blood bank/` beneath the registration form inside the blood bank form card in `src/app/blood-bank/page.tsx` with supporting layout styling in `src/app/blood-bank/BloodBankPage.module.css`. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Impact Dropdown Left Card Removal]: Removed the left-side illustration card beneath the Impact text in the dropdown, leaving the vector art only in the background layer and simplifying the intro column markup/styles in `src/components/header/Navbar.tsx` and `src/app/globals.css`. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Impact Dropdown Professional Readability Pass]: Refined the impact mega-dropdown visuals in `src/app/globals.css` by increasing text contrast, tightening vector artwork placement/scale, and making the category/story cards more opaque and professionally balanced for improved readability. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Impact Dropdown White Panel Conversion]: Converted the impact mega-dropdown surface from translucent blurred glass to a solid white panel in `src/app/globals.css` while preserving the full-visibility vector background layer and existing content/link behavior. Validated with zero diagnostics in modified files. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Impact Dropdown Full-Visibility Vector Background]: Updated the impact mega-dropdown so vector illustrations are no longer rendered as thumbnail tiles under the impact intro card; instead all vector assets are now used directly as the dropdown background layer in `src/app/globals.css` with full visibility (opacity and blending adjusted) while preserving existing links/content in `src/components/header/Navbar.tsx`. Validated with zero diagnostics in modified files. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Impact Vector Imagery + Blood Bank Copy Trim]: Integrated the new vector assets from `public/images/vectors/` into the Impact experience by adding decorative vector tiles in the navbar impact mega-dropdown (`src/components/header/Navbar.tsx` + `src/app/globals.css`) and subtle vector background ambience on impact story pages (`src/components/impact/ImpactStoryPage.module.css`). Also removed the blood-bank helper sentence `Register as a donor for urgent blood coordination in Bahawalpur.` and reshaped the remaining movement copy into a controlled multi-line heading in `src/app/blood-bank/page.tsx` + `src/app/blood-bank/BloodBankPage.module.css` so it renders in 2–3 lines. Validated with zero diagnostics in modified files. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Impact Dropdown SVG Illustration Pass]: Added compact category-specific SVG glyphs and a subtle illustrative badge cluster to the navbar impact mega-dropdown in `src/components/header/Navbar.tsx` and `src/app/globals.css`, so the panel now includes relevant visual illustrations without changing any links, copy, or dropdown behavior. Validated with zero diagnostics in modified files. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Blood Bank Horizontal Intro + Impact Dropdown Typography Boost]: Reworked the blood bank hero intro in `src/app/blood-bank/page.tsx` + `src/app/blood-bank/BloodBankPage.module.css` to use the available horizontal space more intelligently by splitting the headline/lead into a wider horizontal composition instead of stacking all copy into narrow multi-line blocks. Also increased impact mega-dropdown readability in `src/app/globals.css` by scaling up the main heading, category titles, story card text, and link typography while retaining the same panel structure and content. Validated with zero diagnostics in modified files. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Impact Dropdown Colorful Minimal Redesign]: Restyled the navbar impact mega-dropdown in `src/components/header/Navbar.tsx` and `src/app/globals.css` into a lighter, colorful, minimalist panel with soft gradients, pastel-toned category cards, compact story cards, and smoother hover/entry transitions. Preserved all existing dropdown content, links, and interaction behavior while making the panel feel more modern, subtle, and aligned with the rest of the app. Validated with zero diagnostics in modified files. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Blood Donation Parallel Alignment + Visibility Scaling]: Refined blood donation page composition for stronger parallel alignment between left video card and right registration block by moving `Already registered? Contact Team` into the form shell header (removing right-column top offset), introducing a compact `leftIntro` wrapper, tightening left-panel spacing, and increasing video frame height so the media block starts higher with less wasted space (`src/app/blood-bank/page.tsx`, `src/app/blood-bank/BloodBankPage.module.css`). Increased readability across the right panel by scaling form headings, labels, inputs, and CTA dimensions in `src/features/blood-bank/components/BloodBankRequestForm.module.css`. Functionality remains unchanged; validated with zero diagnostics in modified files. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Blood Donation Layout Density Pass]: Optimized blood donation page spacing and vertical utilization by restructuring left-panel top content into a parallel header row (`brand + emergency contacts`) in `src/app/blood-bank/page.tsx`, reducing wasted gaps and preventing the video from being pushed too far down. Tuned `src/app/blood-bank/BloodBankPage.module.css` with stronger panel-height balance (`container` min-height, larger video frame, and full-height form shell) and refined typography/spacing. Increased form visual density in `src/features/blood-bank/components/BloodBankRequestForm.module.css` (larger label/input/button sizes and spacing) so the right side fills space more intelligently while preserving existing functionality and payload behavior. Validated with zero diagnostics in modified files. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Programs Page Visual Redesign - Split Hero + Color Cards]: Redesigned `src/app/programs/page.tsx` to replace the old dark hero shell with a reference-style split layout using the same existing page text and live program data: left-aligned preserved heading/description and right-side colorful animated program tiles sourced from current program titles/categories/icons. Added scoped styling in `src/app/programs/ProgramsPage.module.css` with gradient atmosphere and easing-based card motion. Reworked programs hub presentation in `src/features/programs/components/ProgramsHubClient.tsx` + new `ProgramsHubClient.module.css` to modernize the filter/search panel and results into vibrant color-accent card visuals with transition/easing interactions while preserving original functionality (search, category filtering, and `/programs/[slug]` links) and retaining existing visible copy strings. Validated with zero diagnostics in modified files. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Blood Donation Minimal Copy + Field Simplification]: Refined the donor-UA-inspired blood donation page by removing non-essential helper copy (including call-first/form-second style text), renaming left-panel branding from `donor.psz` to `Paksarzameen Blood Bank`, and moving emergency contact cards to the top of the left panel for immediate visibility in `src/app/blood-bank/page.tsx` + `src/app/blood-bank/BloodBankPage.module.css`. Simplified the registration form in `src/features/blood-bank/components/BloodBankRequestForm.tsx` by removing additional replica requirements (password/repeat-password/extra consent and other non-essential replica fields) and retaining only minimal required donor inputs compatible with the existing backend payload contract (`/api/blood-requests`). Validated with zero diagnostics in modified files. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Blood Donation Page Donor-UA Style Redesign + Video Integration]: Rebuilt `src/app/blood-bank/page.tsx` + `src/app/blood-bank/BloodBankPage.module.css` into a donor.ua-inspired split registration layout (left visual panel + right registration card), integrated the provided blood donation media asset (`/public/videos/blood_donation.mp4`) as the hero visual, and preserved emergency call actions for both coordinators. Restyled `src/features/blood-bank/components/BloodBankRequestForm.tsx` + `BloodBankRequestForm.module.css` to match the referenced clean form aesthetic while keeping the original submission payload and endpoint (`POST /api/blood-requests`) intact; added UI-only replica fields and consent checkbox without changing backend request schema. Validated with zero diagnostics in all modified files. (COPILOT_LOG)
- 2026-04-05 [PSZ Main Web/Impact Banner Visual Redesign + Guinness Label Rename]: Upgraded the shared impact hero banner in `src/components/impact/ImpactStoryPage.tsx` and `src/components/impact/ImpactStoryPage.module.css` with a more stylish modern glass treatment, animated ambient beam/noise layers, and a new animated `Impact in motion` kicker for a catchier but subtle first impression across impact routes. Renamed visible impact content label instances from `GWR` to `Guinness World Record` in `src/content/impact/shared.ts` and `src/content/impact/environmental.ts` while preserving existing `/impact/environmental/gwr` routing and internal keys for backward compatibility. Validated with zero diagnostics in all modified files. (COPILOT_LOG)
- 2026-04-05 [Paksarzameen Store/Stale Chunk Runtime Guard]: Resolved intermittent dev/runtime missing chunk errors (`Cannot find module './8948.js'` from `store/.next/server/webpack-runtime.js` and `/_document`) by clearing stale `store/.next` artifacts and adding an automatic cache cleanup guard via `predev` script in `store/package.json` (`rmSync('.next', { recursive: true, force: true })`) before `next dev`. Verified with successful clean `store` production build after cleanup. (COPILOT_LOG)
- 2026-04-05 [Paksarzameen Store/Hero Pink Overlay Removal]: Removed the remaining rose/pink hero tint layers from `store/src/components/storefront/HeroSection.tsx` by deleting the soft pink gradient overlay and left-side rose glow element, leaving a cleaner untinted banner with only subtle green atmospheric glow. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-04-05 [Paksarzameen Store/Navbar Link Expansion + Label Rename]: Updated store navigation definitions in `store/src/lib/constants.ts` by renaming `Artisans` to `Artisans & Craftsmen` and adding a new navbar field `Customer's Art Gallery` routed to `/customers-art-gallery`. Added new page `store/src/app/customers-art-gallery/page.tsx` as a premium coming-soon experience with contributor and shopping CTAs so the new nav item is fully functional (no 404). Verified with successful `store` production build. (COPILOT_LOG)
- 2026-04-05 [Paksarzameen Store/Navbar Branding CL Removal]: Updated store branding source to ensure navbar uses the actual store logo instead of legacy CL/Commonwealth mark by changing `COMMONWEALTH_LOGO_URL` fallback in `store/src/lib/constants.ts` to local `/paksarzameen_logo.png`, and copied logo asset into `store/public/paksarzameen_logo.png` for stable local/production rendering. Verified with successful clean `store` production build. (COPILOT_LOG)
- 2026-04-05 [Paksarzameen Store/Hero Tint Softening + Category UX Reorder + Designers Section]: Reduced rose/pink overlay intensity in `store/src/components/storefront/HeroSection.tsx` by lightening hero gradient and reducing left glow opacity for a cleaner premium visual. Reworked `store/src/app/categories/[slug]/page.tsx` flow so category pages now present `Make Your Own` first (customization-first CTA), followed by a clearly labeled `Launch Series` products section (category product grid + pagination), and added a new `Designers & Collaborations` future-facing section with `Coming Soon` + waitlist/contact CTA for user-submitted designs and collaborative launches. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-04-03 [Paksarzameen Store/Minimal Accent Softening + Card Overlay Cleanup]: Softened store pink/rose accents for a cleaner minimalist tone by retuning visual tokens and gradients in `store/src/app/globals.css` (`--brand-rose`, `store-section-soft`, and `store-mesh-bg`) and reducing hero rose glow intensity in `store/src/components/storefront/HeroSection.tsx`. Reworked category card text treatment in `store/src/components/storefront/CategoryCard.tsx` from dark boxed caption containers to a professional bottom gradient-shadow overlay with direct text on-image for improved readability and cleaner look. Also lightened customization option chips in `store/src/app/customizations/page.tsx`. Verified with successful clean `store` production build. (COPILOT_LOG)
- 2026-04-03 [Paksarzameen Store/Header Image Swap to store_header]: Replaced the storefront hero header image source in `store/src/components/storefront/HeroSection.tsx` from `/images/commonwealth_header.jpeg` to `/images/store_header.png`, and copied the provided `store_header.png` asset into `store/public/images/` so the nested store app serves it directly in production builds. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-04-03 [Paksarzameen Store/Category Customize CTA + Dedicated Route]: Added a modern category-level customization callout banner in `store/src/app/categories/[slug]/page.tsx` that presents a clear `Customize This Category` action while browsing products, routing users to a new dedicated customization page `store/src/app/customizations/[slug]/page.tsx`. Updated `store/src/app/customizations/page.tsx` category cards to link directly to the new per-category customization route. This preserves product-first browsing and provides explicit one-click access to category customization flow. (COPILOT_LOG)
- 2026-04-03 [Paksarzameen Store/Category Click Product-First Routing]: Fixed category navigation behavior in `store/src/app/categories/[slug]/page.tsx` so clicking a category now always loads that category's product listing first instead of forcing customization-only mode. Removed the customization-first fetch gate, always loads products + count/pagination, and moved category customization UI into an explicit optional section below the product grid for categories that support custom orders. (COPILOT_LOG)
- 2026-04-03 [PSZ Main Web/Heart Section Mobile Fit + Text Width Fix]: Updated mobile Heart of PakSarZameen styling in `src/app/globals.css` to eliminate narrow multiline text collapse and improve member photo framing by changing `.heart-showcase` to `align-items: stretch` on mobile, widening content usage with tighter section side padding, removing image-column max-width constraints, increasing `.heart-image-frame` vertical height, and switching mobile `.heart-member-img` to `object-fit: contain` for better full-photo visibility. Also adjusted mobile info panel alignment/spacing (`.heart-info-col`, `.heart-member-info`, `.heart-info-name`, `.heart-info-quote`) so designation/name/quote utilize full width instead of stacking into overly narrow columns. (COPILOT_LOG)
- 2026-04-03 [PSZ Main Web/Home Mobile Animation + Member Photo Fallback Fix]: Fixed phone-view rendering gap where homepage animations and Heart of PakSarZameen member photos remained hidden when `motion-lite` skipped GSAP initialization by adding `startMobileLiteFallbackAnimations` in `src/features/home/components/HomeClient.tsx` to force-show `scroll-reveal` content, reveal the first member image/info immediately, and run a lightweight timed rotation with counter/progress updates on mobile-lite devices. Added matching CSS fallback visibility/transitions for `.motion-lite .scroll-reveal`, `.motion-lite .heart-member-img`, and `.motion-lite .heart-member-info` in `src/app/globals.css` so hidden default states no longer block mobile rendering. (COPILOT_LOG)
- 2026-04-03 [PSZ Main Web/Blood Bank De-Carded Wide Layout]: Updated `src/app/blood-bank/BloodBankPage.module.css` and `src/features/blood-bank/components/BloodBankRequestForm.module.css` to remove card-based chrome (boxed shells, heavy shadows, rounded panel treatment) and shift the blood-bank page to a wider two-column desktop composition that utilizes left/right viewport space. Refined call rows and form controls into flatter divider-led styling while preserving emergency palette, dialer call actions, bilingual copy, and responsive single-column fallback on smaller screens. (COPILOT_LOG)
- 2026-04-03 [PSZ Main Web/Blood Bank Full UI Rebuild - Emergency Minimal Theme]: Rebuilt the blood bank experience from scratch using CSS Modules by replacing inline-style composition in `src/app/blood-bank/page.tsx` with a new modern minimal structure and emergency-focused visual system in `src/app/blood-bank/BloodBankPage.module.css` (soft emergency gradients, red-accent call cards, concise bilingual emergency messaging, and clean call-first layout). Also rebuilt `src/features/blood-bank/components/BloodBankRequestForm.tsx` styling architecture to class-based CSS with new `src/features/blood-bank/components/BloodBankRequestForm.module.css` for cohesive page-level design, preserving donor registration behavior and mobile responsiveness while aligning with the emergency color direction. (COPILOT_LOG)
- 2026-04-03 [PSZ Main Web/Blood Bank Minimal Bilingual Redesign]: Redesigned `src/app/blood-bank/page.tsx` to a cleaner minimal layout with reduced copy, bilingual English/Urdu emergency messaging, and call-first flow while removing visible phone numbers from UI and keeping direct `tel:` dialer actions via coordinator call buttons. Updated `src/features/blood-bank/components/BloodBankRequestForm.tsx` by renaming “Emergency Case Tracking Form” to “Blood Donation Form”, shifting copy toward donor registration intent, adding concise Urdu labels/copy, and simplifying form visual styling for a lighter minimal presentation. (COPILOT_LOG)
- 2026-04-03 [PSZ Main Web/Impact Full-Width Premium Pass + Instagram Restored]: Revised `src/components/impact/ImpactStoryPage.tsx` and `src/components/impact/ImpactStoryPage.module.css` to restore Instagram embeds in impact story pages via `story.media` rendering (`InstagramEmbed`) and shift layout from narrow boxed cards to a full-width section-based storytelling canvas (wider container, edge-to-edge rhythm, divider-led sections, reduced card chrome, and cleaner premium typography/spacing). Also added safe external/internal related-link handling and retained concise copy shaping while preserving media visibility. (COPILOT_LOG)
- 2026-04-03 [PSZ Main Web/Home Split Heading Wrap Fix]: Fixed the broken heading wrap (`Building Community Wealt` + `h.` on next line) in `src/features/home/components/HomeClient.tsx` by replacing character-level `TextReveal` rendering for the split-section title with a standard `<h2>` (`split-left-heading`), preventing mid-word line breaks while preserving existing section layout and typography. (COPILOT_LOG)
- 2026-04-03 [PSZ Main Web/Impact UI Full Storytelling Redesign]: Reconstructed the shared impact page UI in `src/components/impact/ImpactStoryPage.tsx` and `src/components/impact/ImpactStoryPage.module.css` into a cleaner modern storytelling format applied across all impact routes: single-column narrative flow, reduced visual density, compact copy slicing, capped chapter/outcome/related counts, simplified quick-facts chips, restrained gallery, and lightweight related-story list links instead of dense multi-card/embedded blocks. This pass intentionally minimizes card-heavy sections and keeps visual language consistent with the website’s clean green-accent identity. (COPILOT_LOG)
- 2026-04-03 [PSZ Main Web/Navbar Tagline Removal + Overlap Fix + Hero Cleanup]: Removed the two-line navbar tagline block from `src/components/header/Navbar.tsx` and tightened header spacing/typography in `src/app/globals.css` (logo size, nav link size/gap, store button sizing, and nav padding) to resolve desktop header overlap. Also simplified hero copy in `src/features/home/components/HomeClient.tsx` by removing the `Building dignity through practical community care.` line and the extra Urdu line beneath it from the hero description block. (COPILOT_LOG)
- 2026-04-03 [PSZ Main Web/Impact Section Minimal Reconstruction]: Rebuilt impact story rendering in `src/components/impact/ImpactStoryPage.tsx` to a concise minimalist presentation across all impact pages/sub-pages (including GWR, COP in My City, and related routes) by introducing automatic compact text shortening for intro/summary/chapters/outcomes/closing copy, reducing visible quick facts/highlights/chapters/related cards counts, and removing heavy long-form blocks (hope stories, resources, media, note panel, extra section eyebrow headers) from page output to create a cleaner, catchier, low-text reading flow without rewriting each content record individually. (COPILOT_LOG)
- 2026-04-03 [PSZ Main Web/Hero Label + Navbar Prominence + About Minimalism]: Updated homepage hero top line in `src/features/home/components/HomeClient.tsx` to the Urdu tagline (`تربیت سے تعلیم`) with English companion line (`Nurturing Character Through Education`). Enhanced header identity in `src/components/header/Navbar.tsx` + `src/app/globals.css` by introducing a two-line logo block and stronger modern navigation typography using a new `Sora` display font loaded in `src/app/layout.tsx`. Refactored `src/app/about/page.tsx` into a shorter minimalist composition with concise hero copy, compact mission/vision/method cards, and a simplified CTA; added matching minimalist styles/responsive rules in `src/app/globals.css`. (COPILOT_LOG)
- 2026-04-03 [PSZ Main Web/Hero Text Visibility Race Fix]: Fixed intermittent homepage hero text disappearance in `src/features/home/components/HomeClient.tsx` by preventing GSAP pre-hide from running when intro overlay is already skipped/hidden, centralizing immediate hero text reveal through a shared helper, and adding a timed safety check that force-reveals hero copy if opacity remains near-zero after initialization. (COPILOT_LOG)
- 2026-03-18 [PSZ Main Web/Navbar Intelligent Color Inversion]: Upgraded navbar contrast behavior to be background-aware while scrolling by replacing route-based text color logic in `src/components/header/Navbar.tsx` with runtime surface luminance detection (`elementsFromPoint` + computed background color traversal) and dynamic `light-text`/`dark-text` class switching on scroll/resize/route changes. Refactored navbar styling in `src/app/globals.css` to use CSS variables (`--nav-fg`, `--nav-surface-bg`, `--nav-surface-border`) so logo/links/hamburger/blur shell all flip between black-on-light and white-on-dark consistently, including light-theme overrides. (COPILOT_LOG)
- 2026-03-18 [PSZ Main Web/Navbar Emergency Marquee Removal]: Removed the global emergency contact strip/ticker style block from `src/components/header/Navbar.tsx` on non-home routes (desktop and mobile menu) so emergency urgency is focused on the dedicated `/blood-bank` page instead of appearing as a persistent marquee in navbar chrome. (COPILOT_LOG)
- 2026-03-18 [PSZ Main Web/Blood Bank Emergency Hierarchy Redesign]: Reworked `src/app/blood-bank/page.tsx` to shift hierarchy from form-first to emergency-first by introducing high-contrast urgent hero messaging, oversized click-to-call number cards for Umar Hafeez and Ahmed Amir, and explicit call-first sequencing; moved the form into a secondary tracking section. Updated form intent copy in `src/features/blood-bank/components/BloodBankRequestForm.tsx` (“Emergency Case Tracking Form”, post-call tracking language, and submit CTA text) to avoid implying blood access is gated by form submission. (COPILOT_LOG)
- 2026-03-15 [PSZ Main Web/Blood Bank Backend + Main Admin Panel]: Replaced main-site admin redirect with a native `/admin` module on the main app by removing `src/app/admin/[[...slug]]/route.ts` and adding authenticated admin login + management routes (`src/app/admin/page.tsx`, `src/app/admin/login/page.tsx`, `src/app/api/admin/{login,logout,blood-requests}/...`) using cookie-signed sessions in `src/lib/main-admin-auth.ts` with the same fallback credentials as store auth (`abdullahtanseer@gmail.com` / `CommonWe@lth!`). Implemented Blood Bank backend on the same database via `pg` (`src/lib/db.ts`) and `src/lib/blood-bank.ts` with persistent status workflow (`pending`, `in_progress`, `completed`, `cancelled`), public intake endpoint `src/app/api/blood-requests/route.ts`, public request page `src/app/blood-bank/page.tsx`, and homepage/navbar emergency visibility updates in `src/features/home/components/HomeClient.tsx`, `src/components/header/Navbar.tsx`, and `src/config/site.ts` including emergency contacts (Umar Hafeez, Ahmed Amir). Added schema tracker entries for `blood_bank_requests` in `docs/database/supabase_schema_tracker.md` and verified with successful root production build. (COPILOT_LOG)
- 2026-03-15 [Paksarzameen Store/Sub-option Required + Select Value Simplification]: Added per-sub-option required control in admin customization editor by extending sub-option data with `required` (`store/src/lib/{validations,customizations}.ts`, `store/src/components/admin/CategoryForm.tsx`) and wired storefront required validation to honor sub-option-level mandatory selections in `store/src/components/storefront/CategoryCustomizationPanel.tsx` and `store/src/app/products/[slug]/AddToCartButton.tsx`. Also removed manual `Value` text input from select-value rows in admin; value keys are now derived from label via `slugify` during save to reduce redundant input. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-03-15 [Paksarzameen Store/Customization Image Field Type]: Added `image` as a supported customization field type across validation and parsing (`store/src/lib/{validations,customizations}.ts`), exposed it in admin sub-option type selector in `store/src/components/admin/CategoryForm.tsx`, and implemented customer image upload handling for image-typed groups in both `store/src/components/storefront/CategoryCustomizationPanel.tsx` and `store/src/app/products/[slug]/AddToCartButton.tsx` using `/api/upload` with preview/remove controls. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-03-15 [Paksarzameen Store/Sub-option Value Type Selectors]: Added per-sub-option field type configuration (`select`, `text`, `textarea`, `number`) in admin customization editor `store/src/components/admin/CategoryForm.tsx` so admins can choose how each sub-option captures values. Extended validation and parsing layers (`store/src/lib/validations.ts`, `store/src/lib/customizations.ts`) to persist and read group-level metadata (`fieldType`, `placeholder`, `min`, `max`). Updated storefront customization renderers in `store/src/components/storefront/CategoryCustomizationPanel.tsx` and `store/src/app/products/[slug]/AddToCartButton.tsx` to render each group according to its configured type while preserving select-value price adjustments. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-03-15 [Paksarzameen Store/Admin Customization Suboptions-First UX]: Updated `store/src/components/admin/CategoryForm.tsx` so category customization setup no longer asks admins to choose a field type per option; options now directly collect sub-options/values, and saved payloads are normalized to `fieldType: "select"` with grouped sub-options for consistency. Also updated helper copy and new-option defaults to immediately start with sub-option input flow. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-03-15 [Paksarzameen Store/Fallback Palette + Customization Visibility Pass]: Removed bright fallback artifact risk by retuning skeleton shimmer colors to brand-safe warm neutrals in `store/src/components/ui/skeleton.tsx` and strengthening card media fallback behavior in `store/src/components/storefront/ProductCard.tsx` (normalized image paths plus guaranteed themed background fallback). Tightened navbar customization dropdown source to only show categories that are both customizable and have at least one customization option by extending `/api/categories` counts and filtering in `store/src/components/storefront/Navbar.tsx`. Increased customization UI prominence in `store/src/components/storefront/CategoryCustomizationPanel.tsx` with larger option cards, larger selectable value tiles, larger input controls, and elevated summary card styling for better readability. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-03-15 [Paksarzameen Store/Image Fallback + Two-Step Category Customization UX]: Improved storefront product media resilience by normalizing non-root image paths and adding a guaranteed local background fallback in `store/src/components/storefront/ProductCard.tsx` so cards remain visually populated even when primary image URLs fail. Refactored category-level customization flow in `store/src/components/storefront/CategoryCustomizationPanel.tsx` into a two-step experience: Step 1 shows all available options; selecting one opens Step 2 (query-param-driven `?option=` state) to configure that option's suboptions/values or field input, with back navigation to the options list and retained summary/proceed logic. Updated category page typing to accept `option` search param in `store/src/app/categories/[slug]/page.tsx`. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-03-15 [Paksarzameen Store/Navbar Customizations Dropdown]: Updated `store/src/components/storefront/Navbar.tsx` so `Customizations` no longer routes directly by default on desktop/mobile; it now opens a dynamic dropdown/expand list of currently customizable categories loaded from `/api/categories` and linked to `/categories/[slug]`, with `View All` still available to access `/customizations`. Includes desktop hover+click menu behavior and mobile collapsible sub-menu behavior. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-03-15 [Paksarzameen Store/Flexible Customization Field Types]: Expanded category customization architecture to support admin-defined required/optional field types (`select`, `text`, `textarea`, `number`) without schema migration by storing field config in `CustomizationOption.options` JSON. Updated parser and storefront rendering in `store/src/lib/customizations.ts`, `store/src/app/products/[slug]/AddToCartButton.tsx`, and `store/src/components/storefront/CategoryCustomizationPanel.tsx` to enforce required rules per field type and capture freeform values in cart/checkout payloads. Upgraded admin customization builder in `store/src/components/admin/CategoryForm.tsx` with field type selector, required toggle, placeholders, number min/max controls, and conditional select-value editors. Extended validation compatibility in `store/src/lib/validations.ts`. Verified via successful clean `store` production build after clearing stale `.next`. (COPILOT_LOG)
- 2026-03-15 [Paksarzameen Store/Full Storefront Restyle Pass]: Extended the premium cosmetics-inspired design system across remaining public storefront routes by restyling `store/src/app/{artists/page.tsx,artists/[id]/page.tsx,categories/[slug]/page.tsx,customizations/page.tsx,checkout/page.tsx,checkout/success/page.tsx,checkout/cancel/page.tsx,policies/page.tsx,not-found.tsx,loading.tsx}` to use the new section/card/button typography and palette primitives from `store/src/app/globals.css`; improved card readability with stronger overlays/text-shadow in `store/src/components/storefront/CategoryCard.tsx`; and fixed featured product image visibility resilience in `store/src/components/storefront/ProductCard.tsx` by switching fallback image to local asset plus `unoptimized` for remote URLs. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-03-15 [Paksarzameen Store/Storefront Visual Redesign]: Reworked core storefront presentation to a premium cosmetics-inspired system while preserving existing commerce behavior by introducing new global design tokens/utility classes in `store/src/app/globals.css` (cream/rose/green palette, Cormorant + Manrope typography, reusable section/button/card primitives), redesigning shared storefront components (`store/src/components/storefront/{Navbar,HeroSection,ProductCard,CategoryCard,ArtistCard,Footer}.tsx`), and updating page-level composition on home/products/cart/product-detail (`store/src/app/{page.tsx,products/page.tsx,cart/page.tsx,products/[slug]/page.tsx}`) for responsive spacing, hierarchy, and component consistency. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-03-15 [Paksarzameen Store/Customization Image Visibility + Marquee Removal]: Fixed missing sub-option value imagery in `store/src/components/storefront/CategoryCustomizationPanel.tsx` by restoring `next/image` thumbnails for value chips when an image exists, and removed the global customization marquee by deleting ticker markup from `store/src/components/storefront/Navbar.tsx` plus marquee utility styles from `store/src/app/globals.css`; restored navbar row height to 72px single-row header. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-03-15 [PSZ Main + Store/Policy Processing Timeline]: Updated policy copy in both `store/src/app/policies/page.tsx` and `src/app/policies/page.tsx` to change order processing timeline text from `1–2 business days` to `5–10 business days` in structured content and rendered policy body sections for consistency. (COPILOT_LOG)
- 2026-03-15 [Paksarzameen Store/Marquee Visibility Boost]: Increased customization marquee visibility in `store/src/app/globals.css` (larger strip height, higher contrast gradient background, larger text size, stronger spacing, faster animation, and nowrap text) and adjusted navbar row height in `store/src/components/storefront/Navbar.tsx` to keep total fixed header height consistent. Verified with clean `store` production build after clearing stale `.next` artifacts. (COPILOT_LOG)
- 2026-03-15 [Paksarzameen Store/Clickable Customization Marquee]: Updated the animated customization marquee in `store/src/components/storefront/Navbar.tsx` to act as a direct link to `/customizations`, keeping the ticker behavior while making the global announcement actionable from every store page. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-03-15 [Paksarzameen Store/Customization Marquee]: Added a global animated marquee announcement for customization availability across store pages by updating `store/src/components/storefront/Navbar.tsx` with a slim ticker strip and adding reusable marquee animation utilities in `store/src/app/globals.css`; kept fixed header total height at 72px by balancing navbar and marquee heights to avoid page offset breakage. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-03-15 [Paksarzameen Store/Option-Only Customization Ordering]: Updated category customization flow to remove product selection dependency by converting `store/src/components/storefront/CategoryCustomizationPanel.tsx` into an option/sub-option order builder that enforces required selections, calculates options total, and adds a synthetic category-level custom-order line item before redirecting directly to `/checkout`; updated `store/src/app/categories/[slug]/page.tsx` to run in customization-first mode (show options panel only, hide product grid/pagination when customization options exist). Verified with successful cleaned production build. (COPILOT_LOG)
- 2026-03-15 [Paksarzameen Store/Customizations Billing Page]: Added in-page billing flow for customized orders by creating `store/src/components/storefront/CustomizationBillingPanel.tsx` and embedding it into `store/src/app/customizations/page.tsx`; panel filters cart to customized items only, captures billing details, shows per-line customization selections, and calculates base subtotal + customization subtotal + grand total before launching Stripe via `/api/checkout/session`. Also aligned store cart aggregate pricing to customization-aware unit pricing in `store/src/store/cart.ts`. Verified with successful `store` production build. (COPILOT_LOG)
- 2026-03-15 [Paksarzameen Store/Professional Customization Flow]: Implemented professional add-on style customizations across storefront and checkout by introducing structured per-value pricing in cart payloads (`store/src/types/index.ts` + `store/src/lib/customizations.ts`), enforcing required selection of all customization sub-option groups before add-to-cart in `store/src/app/products/[slug]/AddToCartButton.tsx`, adding value-level price tags in category/product selectors and admin customization editor (`store/src/components/storefront/CategoryCustomizationPanel.tsx`, `store/src/components/admin/CategoryForm.tsx`, `store/src/lib/validations.ts`), propagating add-on pricing to cart/checkout/subtotal/Stripe line-items (`store/src/lib/cart-pricing.ts`, `store/src/app/cart/page.tsx`, `store/src/app/checkout/page.tsx`, `store/src/lib/checkout.ts`, checkout/coupon API schemas), and adding a dedicated customer-facing `/customizations` route with navbar+sitemap wiring (`store/src/app/customizations/page.tsx`, `store/src/lib/constants.ts`, `store/src/app/sitemap.ts`). Verified with successful `store` production build. (COPILOT_LOG)
- 2026-03-14 [Paksarzameen Store/Vercel Build Fix]: Resolved store deployment type-check failures that surfaced during `next build` by updating `store/prisma/seed.ts` to match the current `CustomizationOption` schema (grouped JSON options, no legacy `type` field), tightening fallback typing in `store/src/app/admin/categories/[id]/page.tsx` to the real Prisma payload, and refactoring `store/src/app/products/[slug]/AddToCartButton.tsx` to render grouped select-based customization data instead of the removed text/select type branch. Verified with a successful production build in `store/`. (COPILOT_LOG)
- 2026-03-14 [Paksarzameen Store/Customization Hierarchy + Artisan Pages]: Implemented backend-controlled category customization hierarchy by extending `CustomizationOption` with `subcategory` and `position` in `store/prisma/schema.prisma`, adding validation schemas in `store/src/lib/validations.ts`, creating admin API management endpoint `store/src/app/api/categories/[id]/customizations/route.ts`, extending admin category editing (`store/src/components/admin/CategoryForm.tsx` + `store/src/app/admin/categories/[id]/page.tsx`) to manage subcategories/options/sub-options, and rendering customer-facing customization flow on category pages via `store/src/components/storefront/CategoryCustomizationPanel.tsx` + `store/src/app/categories/[slug]/page.tsx`. Also fixed artisan route consistency and improved storefront viewing by updating artist lookup fallback in `store/src/actions/artists.ts`, aligning artist sitemap URLs to slugs in `store/src/app/sitemap.ts`, and polishing profile/listing UX with loading states in `store/src/app/artists/{page.tsx,loading.tsx,[id]/page.tsx,[id]/loading.tsx`. Updated SQL tracker in `docs/database/supabase_schema_tracker.md`. (COPILOT_LOG)
- 2026-03-14 [PSZ Main + Store/Email Update]: Updated email addresses only in footer/contact information surfaces: store footer contact email set to `storepaksarzameen@gmail.com` in `store/src/components/storefront/Footer.tsx` and main-site contact email set to `foundationpaksarzameen@gmail.com` in `src/config/site.ts`; reverted auth/admin/email-seed/env-default changes to preserve existing auth behavior. (COPILOT_LOG)
- 2026-03-14 [PSZ Main + Store/Brand Rename]: Renamed user-facing branding from “Commonwealth”/“Commonwealth Lab”/“Commonwealth Store” to “Paksarzameen Store” across main app, store app, metadata, nav/footer labels, policies pages, and docs while preserving existing route slugs and technical env/constant keys for compatibility. Also fixed rename-side effects in component symbols and storefront headings. (COPILOT_LOG)
- 2026-03-14 [PSZ Main Web/Premium Homepage Redesign]: Rebuilt `src/features/home/components/HomeClient.tsx` into a modular, Framer Motion-based NGO homepage with an animated gradient hero, rotating mission phrase text, CTA pair (`Join Us`, `Donate`), sticky in-page smooth-scroll section nav with mobile hamburger behavior, scroll-triggered impact counters, dynamic-imported volunteer/journey/projects sections (`src/features/home/components/sections/*.tsx`), optimized landmark storytelling imagery via `next/image`, and an animated final volunteer/donate CTA block; validated with successful production build and sitemap generation. (COPILOT_LOG)
- 2026-03-14 [Paksarzameen Store/Policy Visibility Fix]: Added a native Paksarzameen Store policy route at `store/src/app/policies/page.tsx` (with all required sections and FAQ JSON-LD), created an accessible animated accordion at `store/src/components/storefront/PoliciesFaqAccordion.tsx`, exposed policies in store navigation by adding `Policies` to `store/src/lib/constants.ts` navbar links, switched store footer policy link to local `/policies` in `store/src/components/storefront/Footer.tsx`, and added `/policies` to store sitemap output in `store/src/app/sitemap.ts` so policies/FAQ are visible directly in the Paksarzameen Store app. (COPILOT_LOG)
- 2026-03-14 [Paksarzameen Store/Logo Branding Update]: Wired the newly added Paksarzameen Store logo into store branding by introducing `COMMONWEALTH_LOGO_URL` in `store/src/lib/constants.ts`, using it for favicon/icon/OG/Twitter metadata in `store/src/app/layout.tsx`, replacing the navbar `CL` badge with the logo image in `store/src/components/storefront/Navbar.tsx`, and allowing main-domain logo loading via `store/next.config.mjs` remote image pattern (`paksarzameenwfo.com`). (COPILOT_LOG)
- 2026-03-14 [PSZ Paksarzameen Store/Unified Policies Page]: Added a new ecommerce compliance page at `src/app/policies/page.tsx` titled “Paksarzameen Store Policies & Terms” with semantic sections for Shipping, Return & Replacement, Cancellation, Terms, Privacy, Contact, and FAQ; implemented an accessible animated accordion FAQ component in `src/features/commonwealth-lab/components/PoliciesFaqAccordion.tsx`; injected FAQ JSON-LD schema for SEO; updated footer navigation to include “Policies & Terms” in both `src/components/footer/Footer.tsx` and `store/src/components/storefront/Footer.tsx`; and updated sitemap coverage by adding `/policies` in `next-sitemap.config.js` and `public/sitemap.xml`. (COPILOT_LOG)
- 2026-03-14 [PSZ Main Web/SEO + Discoverability Overhaul]: Implemented global SEO hardening across main app by centralizing official brand/contact/social values in `src/config/site.ts`, updating root metadata + robots + Organization JSON-LD in `src/app/layout.tsx`, optimizing page-level metadata/canonicals/OG/Twitter on core routes (`/`, `/about`, `/get-involved`, `/programs`, `/news`, `/commonwealth-lab`), adding dedicated SEO routes for `/contact`, `/projects`, `/volunteer`, and `/commonwealth`, updating footer/contact surfaces with official phone/address/social links (including Paksarzameen Store Instagram), replacing remaining homepage raw `<img>` with `next/image`, and wiring automated sitemap/robots generation via `next-sitemap` config + `postbuild` script (`next-sitemap.config.js`, `package.json`). (COPILOT_LOG)
- 2026-03-13 [PSZ Main Web/Mobile Startup Optimization]: Fixed the long white-screen startup on phones by bypassing the blocking homepage intro and heavy GSAP/Lenis initialization for mobile/reduced-motion users in `src/features/home/components/HomeClient.tsx`, adding a global boot spinner in `src/app/layout.tsx`, adding a visible loading wheel to `src/app/loading.tsx`, and updating `src/app/globals.css` so mobile shows the hero content immediately instead of waiting for client-side animation setup. (COPILOT_LOG)
- 2026-03-13 [Paksarzameen Store/Admin Dropdown Yellow Fix]: Updated shared select primitives in `store/src/components/ui/select.tsx` to explicit neutral styles (`bg-white`, `border-neutral-200`, neutral focus/hover colors) replacing token-based classes that could resolve to unintended yellow backgrounds, and added WebKit autofill overrides in `store/src/app/globals.css` to force white input/textarea backgrounds in admin forms. (COPILOT_LOG)
- 2026-03-13 [Paksarzameen Store/Admin Variant Naming Cleanup]: Removed remaining admin-facing `gold` variant naming by renaming to `neutral` across `store/src/components/admin/StatsCard.tsx`, `store/src/app/admin/{page.tsx,analytics/page.tsx,products/page.tsx,sales/page.tsx}`, `store/src/components/admin/AdminTable.tsx`, `store/src/components/ui/badge.tsx`, and `store/src/app/globals.css` (`.stat-card-neutral`), with no behavior change to rendered styling. (COPILOT_LOG)
- 2026-03-13 [Paksarzameen Store + Main Web/Accent De-Yellowing]: Removed yellow/amber accent usage from admin warning surfaces by replacing amber utility classes in `store/src/app/admin/page.tsx` and warning icon colors in `store/src/components/admin/StatsCard.tsx`, retuned warning token/gradient in `store/src/app/globals.css` to slate tones, and added Paksarzameen Store-only loading overrides (`commonwealth-loading`) in `src/app/commonwealth-lab/loading.tsx` + `src/app/globals.css` so marketplace loading mockups render with green-neutral shimmer instead of warm/yellow tinting. (COPILOT_LOG)
- 2026-03-13 [Paksarzameen Store/Neon DB Local Setup]: Configured local store environment files (`store/.env.local` and `store/.env`) with Neon PostgreSQL connection variables, synced schema via Prisma db push, regenerated Prisma client, and seeded baseline catalog/admin data so storefront content and admin routes load from live DB locally. (COPILOT_LOG)
- 2026-03-13 [Paksarzameen Store/Admin DB Guard]: Hardened `store/src/app/admin/page.tsx` so dashboard Prisma aggregates no longer crash when `DATABASE_URL` is missing or unavailable; page now renders with safe zero-state values and an in-page configuration warning while preserving normal live metrics when DB is configured. (COPILOT_LOG)
- 2026-03-13 [Local Dev Port Assignment]: Locked local app ports by updating npm scripts so PSZ main web runs on port 3000 in root `package.json` (`dev` and `start`) and the Paksarzameen Store runs on port 3001 in `store/package.json` (`dev` and `start`); verified both endpoints respond with HTTP 200 at localhost:3000 and localhost:3001 concurrently. (COPILOT_LOG)
- 2026-03-13 [Paksarzameen Store/Product Info Per-Item]: Replaced hardcoded product accordion text with per-product fields by adding `materials`, `careInstructions`, and `heritageStory` to `store/prisma/schema.prisma`, extending product validation and admin form inputs in `store/src/lib/validations.ts` + `store/src/components/admin/ProductForm.tsx`, and wiring the product detail accordion to render saved values in `store/src/app/products/[slug]/{page.tsx,ProductAccordion.tsx}`; updated schema tracker with matching SQL columns and regenerated Prisma client. (COPILOT_LOG)
- 2026-03-13 [Paksarzameen Store/Prisma Read Fallback Expansion]: Hardened storefront read actions in `store/src/actions/{categories,artists,products,sales}.ts` to gracefully return empty/null defaults when `DATABASE_URL` is missing or Prisma read queries fail, eliminating runtime crashes on Home/Products/Artists/Category pages in local no-DB environments; verified with successful `store` production build. (COPILOT_LOG)
- 2026-03-13 [Paksarzameen Store/Region Runtime Fallback]: Hardened `store/src/lib/store-regions.ts` against missing `DATABASE_URL` by returning fallback region defaults for read operations when Prisma cannot initialize/query, and added a clear write-path error for region updates when DB env is absent so the app no longer crashes on first render due to region lookup. (COPILOT_LOG)
- 2026-03-13 [Paksarzameen Store/Dependency + Server Boundary Fix]: Installed the nested `store/` app dependencies so client-state packages like `zustand` resolve locally, then fixed a follow-on Next build error by moving cart subtotal helpers into `store/src/lib/cart-pricing.ts` for client-safe reuse and marking `store/src/lib/checkout.ts` as server-only because it depends on Prisma-backed coupon logic. (COPILOT_LOG)
- Initializing project context.
- 2026-03-11 [Paksarzameen Store/Coupons + Accent Cleanup]: Replaced hardcoded coupon configs with a persisted `Coupon` Prisma model plus admin CRUD under `/admin/coupons`, seeded starter coupons, added server-backed coupon validation for checkout, removed coupon suggestion chips so customers must type codes manually, and shifted the store/admin gold accent token path to green-neutral styling in `store/src/app/globals.css`. Prisma client regeneration and a database push are required after pulling the schema changes. (COPILOT_LOG)
- 2026-03-11 [Paksarzameen Store/Admin Product Pricing UX]: Updated `store/src/components/admin/ProductForm.tsx` so the admin product editor now renders explicit price inputs for every active store region, including the default region, instead of only separating base/default pricing from non-default overrides; aligned region guidance copy in `store/src/components/admin/RegionSettingsPanel.tsx` to match the new all-region pricing requirement. (COPILOT_LOG)
- 2026-03-09 [Paksarzameen Store/Force-Dynamic Rendering]: Added `export const dynamic = 'force-dynamic'` to all 16 store pages and routes that query Prisma (/, /artists, /products, /products/[slug], /categories/[slug], /artists/[id], /sitemap.xml, /admin, /admin/analytics, /admin/products, /admin/products/new, /admin/products/[id], /admin/categories, /admin/artists, /admin/sales, /admin/sales/new) to prevent static prerendering at build time; pages now render at request time (SSR) so DATABASE_URL only needs to be available at runtime, not during `next build`. (COPILOT_LOG)
- 2026-03-05 [PSZ Main Web/Phase 1]: Initialized Next.js foundation in `psz_main_web` with Tailwind setup, two-font typography, responsive global Navbar/Footer, and external Paksarzameen Store bridge link wiring via env-configurable URL. (COPILOT_LOG)
- 2026-03-05 [PSZ Main Web/Footer]: Fixed Next.js server runtime error by removing `onSubmit` event handler from newsletter placeholder in `Footer` and keeping it non-interactive for server component compatibility. (COPILOT_LOG)
- 2026-03-05 [PSZ Main Web/Phase 2]: Added typed models (`Program`, `Article`, `ImpactStat`), dummy datasets, async services with shared 500ms delay utility, and schema documentation alignment for future Supabase migration. (COPILOT_LOG)
- 2026-03-05 [PSZ Main Web/Phase 3]: Implemented modular homepage assembly in `src/app/page.tsx` with Hero, mission overview, programs preview, framer-motion impact counters, join CTA, and news preview; wired all data through Phase 2 service layer. (COPILOT_LOG)
- 2026-03-05 [PSZ Main Web/Phase 4]: Added Programs and News hubs with category filters/search, dynamic `[slug]` detail pages with metadata generation, related content/share affordances, and service-layer slug fetch helpers. (COPILOT_LOG)
- 2026-03-05 [PSZ Main Web/Phase 5]: Completed SEO and semantic polish with strict metadata for layout/static pages, improved dynamic metadata consistency, semantic heading refinements in hub components, and verified optimized Next `Image` usage with descriptive alt text. (COPILOT_LOG)
- 2026-03-05 [Handover/Docs]: Added comprehensive developer handover documentation for main web completion and Paksarzameen Store kickoff, and added global footer developer credit text (`Development by 04`). (COPILOT_LOG)
- 2026-03-05 [Paksarzameen Store/Planning]: Added detailed phase-by-phase implementation roadmap at `docs/commonwealth/psz_commonwealth_execution_plan.md`, locking cycle scope (mock checkout, guest flow, local cart persistence, EN-first l10n scaffold) and defining architecture, routing, QA, and documentation gates for execution. (COPILOT_LOG)
- 2026-03-07 [PSZ Main Web/Performance]: Major performance & clean-code pass — (1) Created `LazyVideo` component (`src/components/ui/LazyVideo.tsx`) with IntersectionObserver-based lazy loading and auto-pause for off-screen videos; (2) Replaced all non-hero `<video>` elements with `LazyVideo` to defer ~103 MB of video downloads; (3) Switched all `<img>` tags to `next/image` with AVIF/WebP format support, quality caps, and proper `sizes` hints for approach grid, showcase, impact hero, heart members (~54 MB of PNGs), program placeholders, and testimonial avatars; (4) Removed redundant Google Fonts CSS `@import` (already loaded via `next/font/google`); (5) Removed artificial 500 ms delays from all three service functions (`getArticles`, `getPrograms`, `getImpactStats`); (6) Updated `next.config.ts` with image optimization config (AVIF/WebP, remote patterns for Pexels/Unsplash, 30-day cache TTL), compression, and static asset cache headers; (7) Downscaled external Pexels video URLs from UHD 2560×1440 to 720p; (8) Added CSS `contain: layout style paint` on `[data-scroll-section]` and `content-visibility: auto` on `.dark-bottom` for rendering performance; (9) Extracted inline data arrays (HEART_MEMBERS, SCROLL_TEXT_BLOCKS, PROGRAM_CARDS, TESTIMONIAL_AVATARS) from 1353-line `HomeClient.tsx` into `home.content.ts` reducing component to ~1220 lines; (10) Fixed `info.mp4` → `Info.mp4` filename casing bug that would 404 on Linux/Vercel. Build passes cleanly with zero errors or warnings. (COPILOT_LOG)
- 2026-03-07 [Paksarzameen Store/Marketplace UI]: Implemented complete Paksarzameen Store Marketplace UI with dummy data for presentation — (1) Created `Product` model (`src/lib/models/Product.ts`) with `Product` interface, `CartItem` interface, `ProductCategory` type, and `PRODUCT_CATEGORIES` array; (2) Added 16 dummy products across 4 categories (Traditional Clothing, Handicrafts, Cultural Goods, PSZ Merchandise) in `src/data/products.ts` with Unsplash placeholder images; (3) Built product service layer (`src/lib/services/getProducts.ts`) with `getProducts`, `getProductBySlug`, `getFeaturedProducts`; (4) Created `CartContext` (`src/features/commonwealth-lab/context/CartContext.tsx`) with React Context API for client-side cart state (add/remove/update/clear, computed totals); (5) Built 9 reusable components: `ProductCard`, `ProductGrid`, `ProductGallery`, `CategoryFilter`, `QuantitySelector`, `CartItem`, `CartSummary`, `MarketplaceHero`, `ProductDetailClient`; (6) Implemented 5 page routes under `/commonwealth-lab/`: landing page (hero, featured products, category grid, CTA), product listing (search, category filter, sort, pagination with Suspense boundary), product detail with `generateStaticParams`/`generateMetadata`, cart page, checkout page with form and payment selector; (7) Integrated "Paksarzameen Store" nav link into `site.ts` and updated `Navbar.tsx` slice range; (8) Luxury/premium design with gold (#C4A265) accents, elegant typography, hover animations, and responsive layouts; (9) All product images use `next/image` with proper sizing; (10) Also resolved merge conflict markers across 9 files from prior rebase (home.content.ts, HomeClient.tsx, getArticles.ts, getPrograms.ts, getImpactStats.ts, next.config.ts, globals.css, context.instructions.md, package-lock.json). Build passes cleanly with 36 static pages generated. (COPILOT_LOG)
- 2026-03-07 [PSZ Main Web/Media Optimization]: Comprehensive image and video performance optimization — (1) Created `scripts/optimize-images.mjs` using sharp to batch-compress all 23 local PNG/JPEG images to WebP at multiple responsive sizes (sm 480px, md 960px, lg 1440px, xl 1920px, full), achieving 94.3% reduction (82.46 MB → 4.66 MB); output at `public/images/optimized/` with manifest.json containing paths and blur placeholder base64 data URIs; (2) Created `scripts/generate-video-posters.mjs` to generate dark-gradient WebP poster placeholders for all 4 local MP4 videos, output at `public/videos/posters/`; (3) Enhanced `LazyVideo` component with `poster` prop for instant visual before video loads, `webmSrc` prop for dual MP4+WebM source support, and state-based `<source>` element injection so video data is never downloaded until IntersectionObserver triggers; (4) Created `src/lib/utils/media-helpers.ts` with `getBlurDataURL()`, `getOptimizedImagePath()`, `getOptimizedSizes()`, and `VIDEO_POSTERS` exports for centralized media optimization access; (5) Added `placeholder="blur"` + `blurDataURL` to all member images (×8), approach grid images (×7), founder image, and showcase image in HomeClient.tsx — eliminates CLS and shows instant low-quality preview; (6) Added `poster` attribute to hero `<video>` and all 4 `LazyVideo` instances so users see a frame immediately instead of blank space; (7) Switched footer CSS `background-image` from 18.17 MB `footer.png` to 0.14 MB `footer.webp` (99.2% savings); (8) Extended `next.config.ts` headers with security headers (X-Content-Type-Options, X-Frame-Options, X-XSS-Protection, Referrer-Policy, Permissions-Policy), font caching, video poster caching, and Accept-based Vary header for optimized images; (9) Updated `vercel.json` with CDN caching rules for `/images/optimized/`, `/videos/`, `/videos/posters/`, and `/_next/static/`; (10) Added npm scripts: `optimize:images`, `optimize:posters`, `optimize:all`, and wired `prebuild` to auto-run optimization before `next build`. Build passes cleanly — 36 static pages, zero errors/warnings. (COPILOT_LOG)
- 2026-03-07 [PSZ Main Web/Mobile Responsiveness]: Comprehensive mobile responsiveness fixes across entire site — (1) Fixed horizontal overflow by changing all `100vw` sections to `width: 100%; max-width: 100%` with `overflow-x: hidden` on mobile, expanded covered sections to include split, highlight, overlay-reveal, final-cta, scroll-text-block, impact-text/image; (2) Marketplace 2-column mobile grid: changed `ProductGrid.tsx` from `grid-cols-1 sm:grid-cols-2` to `grid-cols-2` with tighter gap (`gap-x-3 gap-y-6`), updated Paksarzameen Store landing page featured products and category grids to also use `grid-cols-2` at all sizes; (3) Made `ProductCard` compact on mobile with smaller text (`text-xs`), reduced padding (`px-2 pt-3 pb-4`), shorter aspect ratio (`aspect-[3/4]` vs `aspect-[4/5]`), and correct `sizes="50vw"` hint for 2-col layout; (4) Fixed hero text positioning: shifted `.hero-content` up via `top: 45%` at 768px, `top: 42%` at 480px, `top: 40%` at 375px to prevent text appearing too low on mobile; (5) Reduced stack-card (programs section) sizing on mobile: `min-height: 38rem` → `34rem` → `30rem` across 768px/480px/375px breakpoints, illustration panel height `11rem` → `9rem` → `8rem`, content padding tightened, added smaller font sizes for card description/subtitle; (6) Fixed navbar mobile padding from `6rem 5% 3rem` → `2.4rem 5% 1.6rem` to prevent excessive top space; (7) Fixed `MarketplaceHero` from `w-screen` to `w-full` preventing horizontal overflow; (8) Added mobile-appropriate padding to Paksarzameen Store sections (`py-16 sm:py-28`, `px-4 sm:px-6`); (9) Added `padding-inline: 5%` to pinned-video section text on mobile for edge safety. Build passes cleanly — 36 static pages, zero errors. (COPILOT_LOG)
- 2026-03-07 [PSZ Main Web/Scroll Speed & Chapters]: (1) Sped up all GSAP ScrollTrigger scroll-pinned sections — heart section end `total*100%` → `total*55%` and scrub `0.5` → `0.35`; programs section end `cards.length*130%` → `cards.length*80%` and scrub `0.45` → `0.35`; pinned videos end `"bottom top"` → `"70% top"`; canvas section end `"600% top"` → `"350% top"`; (2) Reduced Lenis smooth scroll duration from `1.2` → `0.85` for snappier page feel; (3) Tightened card animation timings — enter `1.6s` → `1.2s`, dot activation `0.4/0.7` → `0.3/0.5`, hold `1.1s` → `0.6s`, push-back `1.1s` → `0.8s`; (4) Removed "Our Impact" header section and image section, replacing them with new "Our Chapters" section (`chapters-section`) showcasing 5 PSZ city chapters: Islamabad (Faisal Mosque SVG, green accent), Bahawalpur (Noor Mahal SVG, gold accent), Hyderabad (Pakka Qila SVG, blue accent), Lahore (Minar-e-Pakistan SVG, red accent), Multan (Shah Rukn-e-Alam SVG, purple accent); (5) Added `Chapter` type and `PSZ_CHAPTERS` data array to `home.content.ts`; (6) Chapters section features animated globe SVG with pulsing pin dots, glassmorphic city cards with landmark SVG icons, accent colour bars, hover glow effects, and index watermarks; (7) Added ~200 lines of chapters CSS with responsive 5→3→2 column grid, `globe-spin` keyframe animation, card hover transforms, and `[data-theme="light"]` overrides; (8) Cleaned up unused `IMAGES.impactHero` reference; (9) Added `.chapters-section` to theme transition smoothing and mobile overflow prevention lists. Build passes — 36 static pages, zero errors. (COPILOT_LOG)
- 2026-03-09 [PSZ Main Web/Admin Routing]: Fixed admin access mismatch between main site and separate store app by adding `src/app/admin/[[...slug]]/route.ts` redirect logic to forward `/admin` and nested admin paths to `siteConfig.commonwealthUrl/admin...`; updated `Navbar` Paksarzameen Store button to open the configured external store URL in a new tab (`target="_blank"`, `rel="noopener noreferrer"`) so users land in the correct app instead of a main-site 404. (COPILOT_LOG)
- 2026-03-09 [PSZ Main Web/Paksarzameen Store URL Defaults]: Updated `src/config/site.ts` to use environment-aware `commonwealthUrl` fallback (`http://localhost:3001` in development, `https://store.paksarzameenwfo.com` in production) so admin redirects and bridge links remain valid even when `NEXT_PUBLIC_COMMONWEALTH_URL` is not set. (COPILOT_LOG)
- 2026-03-09 [Paksarzameen Store/Admin Credentials]: Updated store admin defaults to requested login (`admin@gmail.com` / `admin123`) in `store/prisma/seed.ts`, `store/.env.example`, and `store/.env.local`; database reseed attempt was blocked because `DATABASE_URL` is not yet configured in local env. (COPILOT_LOG)
- 2026-03-09 [Paksarzameen Store/Auth Login Fix]: Resolved repeated `401` invalid credentials path by normalizing credential email input (`trim().toLowerCase()`) in `store/src/lib/auth.ts` before Prisma lookup, and making `store/prisma/seed.ts` deterministically `upsert` the admin account with an updated hashed password on every seed run so requested credentials remain valid after reseeding. (COPILOT_LOG)
- 2026-03-10 [Paksarzameen Store/Production Auth Env Hardening]: Fixed deployed admin sign-in server errors by centralizing auth env resolution in `store/src/lib/auth-env.ts`, explicitly feeding the resolved secret into both `next-auth` and `getToken` middleware, adding `AUTH_SECRET`/`AUTH_URL` fallback support, routing NextAuth errors back to `/admin/login`, and preserving `callbackUrl` on credential sign-in so production redirects remain stable behind Vercel/custom-domain setups. (COPILOT_LOG)
- 2026-03-10 [Paksarzameen Store/Login Error Messaging]: Updated `store/src/app/admin/login/page.tsx` to detect `error=Configuration` from NextAuth and show a deployment-focused message on the login form so production auth misconfiguration is distinguishable from invalid credentials. (COPILOT_LOG)
- 2026-03-10 [Paksarzameen Store/Hardcoded Auth Fallback]: Removed store auth dependence on deployment-only auth env vars by adding a code-level default secret in `store/src/lib/auth-env.ts`, introduced hardcoded admin credential fallback (`admin@gmail.com` / `admin123`) in `store/src/lib/auth.ts` ahead of Prisma lookup, and updated the admin login UI copy in `store/src/app/admin/login/page.tsx` to surface the default access path instead of env-specific remediation. (COPILOT_LOG)
- 2026-03-10 [Paksarzameen Store/Admin Credential Alignment]: Updated the hardcoded auth fallback in `store/src/lib/auth-env.ts`, the admin login hint in `store/src/app/admin/login/page.tsx`, and seed defaults in `store/prisma/seed.ts` to use `abdullahtanseer@gmail.com` / `CommonWe@lth!` so runtime fallback and future reseeds stay aligned. (COPILOT_LOG)
- 2026-03-10 [Paksarzameen Store/Upload Fallback Hardening]: Fixed admin image upload failures by adding code-level Cloudinary fallback configuration in `store/src/lib/cloudinary.ts`, wrapping `store/src/app/api/upload/route.ts` in structured error handling so server failures return JSON instead of opaque 500s, and simplifying `store/src/components/admin/ImageUploader.tsx` to surface the actual API error returned by the server. (COPILOT_LOG)
- 2026-03-11 [Paksarzameen Store/Product Availability UI]: Replaced editable product stock counts with a boolean availability flow across `store/src/lib/validations.ts`, `store/src/components/admin/ProductForm.tsx`, API routes, store actions, admin listings, dashboard, analytics, and storefront purchase UI; availability now maps to the existing `stock` column internally (`1` for available, `0` for sold out) to avoid a breaking database migration while still rendering sold-out behavior consistently. (COPILOT_LOG)
- 2026-03-11 [Paksarzameen Store/Billing + Stripe Checkout]: Replaced the cart WhatsApp completion path with a proper billing flow by adding `/checkout`, Stripe session creation at `store/src/app/api/checkout/session/route.ts`, hardcoded coupon support in `store/src/lib/coupons.ts`, pricing helpers in `store/src/lib/checkout.ts`, success/cancel return pages, Stripe env documentation in `store/.env.example`, and implementation notes in `docs/store/stripe_checkout_impl.md`. Payments are restricted to card-only Stripe Checkout sessions for advance payment. (COPILOT_LOG)
- 2026-03-11 [Paksarzameen Store/Regional Pricing]: Added automatic market detection for Pakistan, the United States, and the United Kingdom by introducing request/cookie-based region helpers in `store/src/lib/pricing.ts` and `store/src/lib/pricing-server.ts`, setting a persistent `psz-region` cookie in `store/src/middleware.ts`, converting storefront/cart/checkout display pricing to regional currencies, and creating Stripe Checkout sessions in the detected market currency while keeping PKR as the catalog source of truth. (COPILOT_LOG)
- 2026-03-11 [Paksarzameen Store/Admin-Managed Region Pricing]: Replaced hardcoded region availability with persisted `StoreRegion` and `ProductRegionPrice` models in `store/prisma/schema.prisma`, added admin region activation/default controls in `store/src/app/admin/settings/page.tsx`, required product forms/APIs to capture prices for every active non-default region, and updated storefront/cart/checkout pricing to use explicit saved regional product prices instead of exchange-rate conversion. Prisma client regeneration is required after pulling these schema changes, and the database needs `prisma db push` before the new tables will exist. (COPILOT_LOG)
- 2026-03-09 [Paksarzameen Store/Admin Runtime + UI Redesign]: Fixed Next.js runtime error caused by passing non-serializable `render` callbacks from server admin pages into client `AdminTable` by replacing table API with serializable column kinds (`text`, `badge`, `image`) and preformatted row data in `store/src/app/admin/{products,categories,artists,sales}/page.tsx`; redesigned shared storefront/admin visuals to align with PSZ + Paksarzameen Store aesthetic across `store/src/app/globals.css`, `store/src/components/storefront/{Navbar,Footer}.tsx`, `store/src/app/admin/layout.tsx`, `store/src/components/admin/{AdminSidebar,StatsCard,AdminTable}.tsx`, `store/src/app/admin/login/page.tsx`, and tuned `store/src/components/ui/button.tsx` variants for consistent premium styling. Type-check passes (`npx tsc --noEmit`, exit 0). (COPILOT_LOG)
- 2026-03-09 [Paksarzameen Store/Deployment Env Safety]: Removed tracked `store/.env` from git and expanded root `.gitignore` to exclude `.env`, `.env.*`, and `store/.env` so Vercel uses project-managed environment variables instead of repository secrets during build/runtime. (COPILOT_LOG)
- 2026-03-09 [PSZ Main Web/Build Scope Isolation]: Fixed root Vercel build failure caused by the main Next.js app type-checking nested `store/` files (`store/prisma/seed.ts`) by excluding `store/**/*` in root `tsconfig.json` and constraining root Next ESLint build scope to `src` via `next.config.ts`. (COPILOT_LOG)
- 2026-04-11 [Paksarzameen Store/Manual Gallery Signup + Admin Approvals Backend]: Replaced customer gallery Google signup CTA flow with manual signup (full name + email) by introducing secure DB-backed manual auth/session models in `store/prisma/schema.prisma` (`GalleryManualSignup`, `GalleryManualSession`), adding manual auth resolver `store/src/lib/manual-gallery-auth.ts`, and updating manual signup/signout APIs (`store/src/app/api/gallery/user/manual-{signup,signout}/route.ts`) to issue/revoke tokenized httpOnly sessions. Wired manual-session access into upload/user gallery APIs and pages (`store/src/app/api/gallery/{upload,user}/route.ts`, `store/src/app/{login,upload-art}/page.tsx`) and restyled manual signup form for store-consistent UI (`store/src/features/gallery/components/ManualSignupForm.tsx`). Added admin approval backend and UI via new admin-only APIs (`store/src/app/api/gallery/admin/submissions/**`) and new admin route/table (`store/src/app/admin/gallery/page.tsx`, `store/src/components/admin/GalleryApprovalsTable.tsx`) with sidebar navigation entry. Updated schema tracker and store implementation docs accordingly. (COPILOT_LOG)
